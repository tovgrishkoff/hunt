# 🔍 АЛГОРИТМЫ ПОИСКА ПО КЛЮЧЕВЫМ СЛОВАМ И ФИЛЬТРАЦИИ СПАМА

## 📋 Оглавление
1. [Общая схема обработки сообщений](#общая-схема-обработки-сообщений)
2. [Уровень 1: Быстрая проверка `is_spam()`](#уровень-1-быстрая-проверка-isspam)
3. [Уровень 2: Критическая проверка `is_critical_spam_only()`](#уровень-2-критическая-проверка-is_critical_spam_only)
4. [Уровень 3: Полная проверка `is_spam_message()`](#уровень-3-полная-проверка-is_spam_message)
5. [Гибридная классификация `_hybrid_classify_message()`](#гибридная-классификация-_hybrid_classify_message)
6. [Компиляция паттернов `_compile_patterns()`](#компиляция-паттернов-_compile_patterns)
7. [Структура паттернов](#структура-паттернов)

---

## 🔄 Общая схема обработки сообщений

```
┌─────────────────────────────────────────────────────────────┐
│  user_monitor_bot.py: @client.on(events.NewMessage())       │
│  Получение сообщения из Telegram чата                       │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────┐
│  УРОВЕНЬ 1: is_spam() (быстрая проверка)                    │
│  📍 user_monitor_bot.py, строка 43-105                      │
│  ✅ Продажа документов (СНИЛС, ИНН, права)                  │
│  ✅ Реклама обмена валют (ХАНИ МАНИ)                        │
│  ✅ Много флагов стран + документы                          │
│  ✅ Телефонные номера в контексте документов/валют          │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼ (если не спам)
┌─────────────────────────────────────────────────────────────┐
│  monitor.process_message_from_subscriber()                  │
│  📍 monitor.py, строка 3002                                 │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────┐
│  УРОВЕНЬ 2: is_critical_spam_only()                         │
│  📍 monitor.py, строка 1423-1766                            │
│  ✅ Базы данных и персональные данные                       │
│  ✅ Наркотики и подозрительные вещества                     │
│  ✅ Звезды и приглашения                                    │
│  ✅ Проверка/верификация                                    │
│  ✅ Регистрация бизнеса, банки, пробив                      │
│  ✅ Работа/подработка, доставка заказов                     │
│  ✅ Дизайнеры, менеджеры, ассистенты                        │
│  ✅ Карты (пушкинская карта, снятие с карты)                │
│  ✅ НДС/чеки/бумажный НДС                                   │
│  ✅ Реклама ботов и форумов                                 │
│  ✅ Инфографика/дизайнеры                                   │
│  ✅ Платные тесты и входные тесты                           │
│  ✅ Продажа поддельных документов                           │
│  ✅ Реклама обмена валют (ХАНИ МАНИ и подобные)             │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼ (если не критический спам)
┌─────────────────────────────────────────────────────────────┐
│  УРОВЕНЬ 3: is_spam_message() (полная проверка)             │
│  📍 monitor.py, строка 1768-1967                            │
│  ✅ Блокировка сообщений от ботов и сервисов                │
│  ✅ Ассистент/менеджер + маркетплейс/WB/Ozon                │
│  ✅ Работа на WB/маркетплейсах                              │
│  ✅ Карты (пушкинская карта)                                │
│  ✅ Telegram-продвижение                                    │
│  ✅ Перевозчик/Курьер с высокой зарплатой                   │
│  ✅ Работа для активных/собрать мебель                      │
│  ✅ Новогодняя доставка/коробки                             │
│  ✅ Обмен валют с "накину процент"                          │
│  ✅ Новогодняя тема для баланса такси                       │
│  ✅ GSM-телефония/реклама телефонии                         │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼ (если не спам)
┌─────────────────────────────────────────────────────────────┐
│  ГИБРИДНАЯ КЛАССИФИКАЦИЯ: _hybrid_classify_message()        │
│  📍 monitor.py, строка 177-1400 (приблизительно)            │
│  ✅ Ранняя проверка инфраструктуры (ТЦ, магазины)           │
│  ✅ Приветственные сообщения ботов                          │
│  ✅ Информационные сообщения со списками каналов            │
│  ✅ Реклама чатов/каналов                                   │
│  ✅ Проверка паттернами (NICHES_KEYWORDS)                   │
│  ✅ AI классификатор (если паттерны не нашли ниши)          │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────┐
│  Отправка сообщения подписчикам                             │
│  (фильтрация по странам, нишам, подпискам)                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚀 Уровень 1: Быстрая проверка `is_spam()`

**Расположение**: `user_monitor_bot.py`, строки 43-105  
**Назначение**: Очень быстрая проверка на очевидный спам **ДО** всех остальных проверок

### Алгоритм:

```python
def is_spam(text: str) -> bool:
    # 1. Проверка на продажу документов
    spam_keywords = [
        'мед книжка', 'мед карта', 'снилс', 'инн', 'корочка сварщика',
        'трудовой договор', 'студенческий билет', 'предоставлю услуги по',
        'рус права', 'узб права', 'тадж права', ... (все страны)
    ]
    
    # 2. Проверка комбинаций: много флагов стран + документы = спам
    flag_count = количество_флагов_стран(🇷🇺🇺🇿🇹🇯...)
    document_keywords_count = количество_ключевых_слов_документов
    if flag_count > 5 and document_keywords_count > 3:
        return True  # Спам
    
    # 3. Проверка на рекламу обмена валют
    currency_spam_keywords = [
        'хани мани', 'hani mani', 'безопасные переводы',
        'нужно поменять валюту', 'твоё решение', ...
    ]
    
    # 4. Проверка на телефонные номера в контексте документов/валют
    if есть_телефонный_номер and (document_keywords_count > 2 or flag_count > 3):
        return True  # Спам
    
    return False
```

### Что блокирует:
- ✅ Продажа документов (СНИЛС, ИНН, права, паспорта, дипломы)
- ✅ Комбинации стран + документы (🇷🇺рус права💳-пас📕)
- ✅ Реклама обмена валют (ХАНИ МАНИ, безопасные переводы)
- ✅ Телефонные номера в контексте документов/валют

### Преимущества:
- ⚡ **Очень быстрая** - блокирует очевидный спам до дорогих операций
- 🎯 **Высокая точность** - блокирует только очевидный спам
- 📉 **Снижает нагрузку** - не доходит до более сложных проверок

---

## 🔥 Уровень 2: Критическая проверка `is_critical_spam_only()`

**Расположение**: `monitor.py`, строки 1423-1766  
**Назначение**: Проверка на **критический спам** (базы данных, наркотики, мошенничество)

### Категории спама:

#### 1. **Базы данных и персональные данные** (строка 1441-1461)
```python
databases_spam_patterns = [
    r'баз[аы].*паспортн', r'паспортн.*данн', r'оптовая.*продажа.*работ',
    r'с\s+инн', r'выборки.*оператор', r'продам.*баз[аы]',
    ...
]
```

#### 2. **Наркотики** (строка 1464-1475)
```python
drugs_spam_patterns = [
    r'наркотик', r'марихуана', r'кокаин', r'героин',
    r'амфетамин', r'экстази', r'лсд', r'мефедрон', ...
]
```

#### 3. **Звезды и приглашения** (строка 1478-1486)
```python
stars_spam_patterns = [
    r'кому\s+нужны\s+звезды', r'нужны\s+звезды', r'в\s+боте\s+дают',
    r'за\s+\d+\s+приглаженного\s+человека', ...
]
```

#### 4. **Проверка/верификация** (строка 1489-1497)
```python
verification_spam_patterns = [
    r'не\s+прошли\s+проверку', r'напишите\s+боту\s+в\s+лс',
    r'для\s+получения\s+доступа', r'решите\s+эту\s+капчу', ...
]
```

#### 5. **Регистрация бизнеса, банки, пробив** (строка 1500-1515)
```python
business_registration_spam_patterns = [
    r'регистраци[яи].*бизнес', r'регистраци[яи].*ооо',
    r'открыти[ея].*счет', r'пробив.*по.*линии', ...
]
```

#### 6. **Работа/подработка** (строка 1518-1529)
```python
job_spam_patterns = [
    r'нужны\s+бабки', r'доставка\s+заказов',
    r'от\s+\d+\s+000\s+в\s+день', r'опыт\s+не\s+нужен', ...
]
```

#### 7. **Дизайнеры, менеджеры, ассистенты** (строка 1532-1543)
```python
designer_spam_patterns = [
    r'ищ[ауе].*дизайнер', r'нужен.*дизайнер',
    r'дизайнер.*(резюме|портфолио)', r'дизайнер.*от\s+18', ...
]
```

#### 8. **Работа/подработка (детально)** (строка 1546-1581)
```python
work_spam_patterns = [
    r'требуются.*помощник', r'работа.*удалённ',
    r'сотрудник.*сдэк', r'работа.*склад', ...
]
```

#### 9. **Карты** (строка 1584-1592)
```python
card_spam_patterns = [
    r'пушкинск[аяой].*карт', r'сним[ау].*карт',
    r'с\s+ваш[ей].*карт', ...
]
```

#### 10. **НДС/чеки** (строка 1595-1613)
```python
vat_spam_patterns = [
    r'бумажн[ыйые].*ндс', r'чеки.*ндс',
    r'гаранти[яи].*услуг.*\d+\s*год', ...
]
```

#### 11. **Реклама ботов и форумов** (строка 1616-1632)
```python
bot_ad_patterns = [
    r'ваш\s+ботя', r'baliforum\.ru', r'saleschatikbot', ...
]
```

#### 12. **Инфографика/дизайнеры** (строка 1635-1643)
```python
infographic_spam_patterns = [
    r'кто\s+занимается.*инфографик', r'инфографик.*отпишите', ...
]
```

#### 13. **Платные тесты** (строка 1646-1658)
```python
paid_test_patterns = [
    r'тест.*оплат', r'входн[ойой].*тест',
    r'пропуск.*за.*\d+', ...
]
```

#### 14. **Перевозчик с высокой зарплатой** (строка 1661-1666)
```python
if 'перевозчик' in text:
    if есть_высокая_зарплата(100000+) and 'пиши в лс':
        return True  # Спам
```

#### 15. **Продажа поддельных документов** (строка 1695-1727)
```python
fake_documents_spam_patterns = [
    r'предоставлю\s+услуги\s+по', r'получению\s+водительск[их]{0,2}\s+прав',
    r'мед\s+книжк', r'снилс', r'инн', ...
]
# + проверка на много флагов стран + документы
```

#### 16. **Реклама обмена валют (ХАНИ МАНИ)** (строка 1729-1762)
```python
currency_exchange_spam_patterns = [
    r'хани\s+мани', r'hani\s+mani', r'безопасн[ыеые].*денежн[ыеые].*перевод',
    r'нужно\s+поменять\s+валют', r'тво[её]?\s+решение', ...
]
# + проверка на много эмодзи + валют + ссылки
```

---

## 🛡️ Уровень 3: Полная проверка `is_spam_message()`

**Расположение**: `monitor.py`, строки 1768-1967  
**Назначение**: Полная проверка на спам (боты, маркетплейсы, работа)

### Категории:

#### 1. **Блокировка ботов и сервисов** (строка 1774-1797)
```python
blocked_usernames = [
    'chatkeeperbot', 'no_joinbot', 'do4urobot',
    'saleschatikbot', 'baliarenda_group', ...
]
bot_indicators = ['bot', 'robot', 'support', 'service', ...]
```

#### 2. **Ассистент/менеджер + маркетплейс** (строка 1812-1844)
```python
assistant_manager_patterns = [
    r'ассистент[аеы]?.*маркетплейс', r'ассистент[аеы]?.*wb',
    r'менеджер[аеы]?.*ozon', r'менеджер[аеы]?.*(на|для).*(озон|ozon).*(и|или).*(вб|wb)',
    ...
]
```

#### 3. **Работа на WB/маркетплейсах** (строка 1847-1862)
```python
wb_spam_patterns = [
    r'стажер.*на\s+(wb|вб)', r'работа\s+на\s+(wb|вб)',
    r'карточк.*товар', r'отвечать.*отзыв.*(wb|вб)', ...
]
```

#### 4. **Карты (пушкинская)** (строка 1865-1873)
```python
card_spam_patterns = [
    r'пушкинск[аяой].*карт', r'сним[ау].*карт', ...
]
```

#### 5. **Telegram-продвижение** (строка 1876-1884)
```python
telegram_promo_patterns = [
    r'telegram-?продвижени[ея]', r'рассылк[иа].*высоким\s+отклик[ом]',
    r'боты\s+и\s+скрипты.*автоматизаци[яи]', ...
]
```

#### 6. **Перевозчик/Курьер** (строка 1887-1891)
```python
if 'перевозчик' and высокая_зарплата and 'пиши':
    return True  # Спам
```

#### 7. **Работа для активных/собрать мебель** (строка 1894-1910)
```python
work_active_patterns = [
    r'работа\s+для\s+активн', r'собрать\s+мебель', ...
]
# + проверка на зарплату + "пиши"
```

#### 8. **Новогодняя доставка/коробки** (строка 1913-1928)
```python
delivery_spam_patterns = [
    r'новогодн[аяую].*доставк', r'коробк[иы].*без\s+погрузк', ...
]
# + проверка на зарплату + "деньги в день работы"
```

#### 9. **Обмен валют с "накину процент"** (строка 1931-1936)
```python
if 'купить usdt' and 'наличные' and 'накину процент':
    return True  # Спам
```

#### 10. **Новогодняя тема для баланса такси** (строка 1939-1948)
```python
if 'новогодняя тема для баланса такси' and (оплата or 'пиши'):
    return True  # Спам
```

#### 11. **GSM-телефония** (строка 1951-1967)
```python
gsm_telephony_patterns = [
    r'gsm[-\s]?телефони[яи]', r'gsm.*дозвон',
    r'статистик[ау].*звонк', ...
]
```

---

## 🎯 Гибридная классификация `_hybrid_classify_message()`

**Расположение**: `monitor.py`, строки 177-1400 (приблизительно)  
**Назначение**: Классификация сообщений по нишам (паттерны + AI)

### Алгоритм:

```
1. Ранняя проверка инфраструктуры (ТЦ, магазины, аптеки)
   └─> Если нет слов про жилье → отклоняем

2. Приветственные сообщения ботов
   └─> Отклоняем

3. Информационные сообщения со списками каналов
   └─> Если много @username (>=5) и эмодзи (>=10) → отклоняем

4. Реклама чатов/каналов
   └─> Отклоняем

5. Проверка паттернами (NICHES_KEYWORDS)
   ├─> Компиляция паттернов при старте (_compile_patterns)
   ├─> Проверка в порядке приоритета:
   │   ├─> "Обмен валют"
   │   ├─> "Продажа недвижимости"
   │   ├─> "Сдача недвижимости"
   │   └─> Остальные ниши
   │
   ├─> Для каждой ниши:
   │   ├─> Проверка паттернов (regex)
   │   ├─> Фильтр болтовни (Chatter Filter)
   │   └─> Проверка исключений (blacklist)
   │
   └─> Если найдены ниши → возвращаем результат

6. AI классификатор (если паттерны не нашли ниши)
   └─> Используется только если паттерны не сработали
   └─> Экономия API
```

### Специальные проверки:

#### **Фильтр болтовни (Chatter Filter)** (строка 520-542)
```python
chatter_keywords = [
    r'ага\s', r'хаха', r'лол', r'😂',
    r'нашел\s+видео', r'поделиться', r'истори[яю]', ...
]
# Если сообщение похоже на болтовню и НЕТ явных слов "ищу/цена/портфолио"
# → Отклоняем
```

#### **Исключения для недвижимости** (строка 548-599)
```python
# Блокируем, если есть:
- Работа/услуги/маркетинг
- Крипта и обмен
- Одежда и вещи
- Визы
- Финансовый спам
- Торговые центры (если нет слов про жилье)
```

#### **Порядок проверки ниш** (строка 502-506)
```python
priority_niches = ["Обмен валют", "Продажа недвижимости", "Сдача недвижимости"]
other_niches = все_остальные_ниши
ordered_niches = priority_niches + other_niches
```

---

## ⚡ Компиляция паттернов `_compile_patterns()`

**Расположение**: `monitor.py`, строки 153-175  
**Назначение**: Превращает текстовые паттерны в объекты Regex **один раз при старте**

### Алгоритм:

```python
def _compile_patterns(self):
    self.compiled_patterns = {}
    
    for niche, patterns_list in NICHES_KEYWORDS.items():
        self.compiled_patterns[niche] = []
        for pattern in patterns_list:
            # Компилируем regex с флагом re.IGNORECASE (нечувствительно к регистру)
            compiled = re.compile(pattern, re.IGNORECASE)
            self.compiled_patterns[niche].append(compiled)
    
    logger.info(f"⚡ Паттерны скомпилированы. Всего ниш: {len(self.compiled_patterns)}")
```

### Преимущества:
- ⚡ **Быстро** - паттерны компилируются один раз, а не каждый раз при проверке
- 💾 **Экономия памяти** - используется один объект Regex для всех проверок
- 🎯 **Точность** - одинаковые паттерны используются везде

---

## 📚 Структура паттернов

### Файл: `patterns.py`

#### 1. **NICHES_KEYWORDS** (ключевые слова для ниш)

Структура:
```python
NICHES_KEYWORDS = {
    "Фотограф": [
        r"фотограф", r"фотосесси", r"фотосъемк",
        r"(порекомендуйте|кто\s+умеет).{0,100}(фотограф|фотосесси)",
        ...
    ],
    "Видеограф": [
        r"видеограф", r"видеосъемк", r"видеомонтаж",
        ...
    ],
    "Сдача недвижимости": [
        r"сдам", r"сниму", r"аренд.*жиль",
        ...
    ],
    ...
}
```

#### 2. **SPAM_FILTERS** (фильтры спама)

Структура:
```python
SPAM_FILTERS = {
    "min_message_length": 20,  # Минимальная длина сообщения
    "max_special_chars": 5,    # Максимальное количество спецсимволов
    "max_emojis": 3,           # Максимальное количество эмодзи
    "spam_keywords": [         # Ключевые слова спама
        "криптовалюта",
        "бинарные опционы",
        "быстрый заработок",
        ...
    ],
    ...
}
```

---

## 🎯 Рекомендации по улучшению

### 1. **Улучшение поиска по ключевым словам**

#### Проблемы:
- ❌ Паттерны могут пропускать синонимы
- ❌ Не учитывается контекст сообщения
- ❌ Нет проверки на опечатки

#### Решения:
- ✅ Добавить больше синонимов в паттерны
- ✅ Использовать морфологический анализ (pymorphy2)
- ✅ Добавить проверку на опечатки (fuzzy matching)
- ✅ Улучшить контекстный анализ (AI классификатор)

### 2. **Улучшение фильтрации спама**

#### Проблемы:
- ❌ Новые виды спама проходят через фильтры
- ❌ Слишком строгие фильтры могут блокировать легитимные сообщения
- ❌ Нет обучения на новых примерах спама

#### Решения:
- ✅ Добавить машинное обучение для классификации спама
- ✅ Регулярно обновлять списки спам-паттернов
- ✅ Добавить систему обратной связи (пользователи помечают спам)
- ✅ Использовать эвристики (много эмодзи + ссылок = спам)

### 3. **Оптимизация производительности**

#### Проблемы:
- ❌ Много проверок regex может замедлить обработку
- ❌ AI классификатор вызывается даже когда не нужен

#### Решения:
- ✅ Улучшить порядок проверок (быстрые сначала)
- ✅ Использовать кэширование результатов
- ✅ Параллельная обработка сообщений
- ✅ Использовать более эффективные алгоритмы поиска

### 4. **Конкретные улучшения**

#### Для "Фотограф":
```python
# Добавить больше синонимов:
- "порекомендуйте фотографа" ✅ (уже есть)
- "нужен человек для съемки" ✅ (уже есть)
- "ищу того, кто фотографирует" ❌ (можно добавить)
- "кто делает фотосессии" ❌ (можно добавить)
```

#### Для спама:
```python
# Добавить проверку на новые виды спама:
- Проверка на множественные ссылки
- Проверка на CAPS LOCK (много заглавных букв)
- Проверка на повторяющиеся символы (!!!, ???)
- Проверка на короткие сообщения с ссылками
```

---

## 📊 Статистика и мониторинг

### Что отслеживать:
1. **Эффективность фильтров спама**:
   - Сколько сообщений заблокировано на каждом уровне
   - Сколько ложных срабатываний
   - Какие паттерны чаще всего срабатывают

2. **Эффективность поиска по нишам**:
   - Сколько сообщений найдено паттернами
   - Сколько сообщений найдено AI
   - Какие ниши чаще всего находятся

3. **Производительность**:
   - Время обработки одного сообщения
   - Использование CPU/памяти
   - Количество вызовов API

---

## 🔧 Как добавить новый паттерн

### Для ниш (NICHES_KEYWORDS):

1. Открыть файл `patterns.py`
2. Найти нужную нишу в `NICHES_KEYWORDS`
3. Добавить новый паттерн (regex):
```python
"Фотограф": [
    r"фотограф",  # Существующие паттерны
    r"фотосесси",
    r"новый_паттерн_здесь",  # Новый паттерн
    ...
],
```

4. Перезапустить монитор (паттерны компилируются при старте)

### Для спама (is_critical_spam_only):

1. Открыть файл `monitor.py`
2. Найти функцию `is_critical_spam_only()` (строка 1423)
3. Добавить новую категорию спама или расширить существующую:
```python ы
# КРИТИЧЕСКИЙ СПАМ X: Новый вид спама
new_spam_patterns = [
    r'новый_паттерн_1',
    r'новый_паттерн_2',
    ...
]
for pattern in new_spam_patterns:
    if re.search(pattern, text_lower, re.IGNORECASE):
        logger.info(f"🚫🚫🚫 КРИТИЧЕСКИЙ СПАМ (новый вид): найдено '{pattern}'...")
        return True
```

4. Перезапустить монитор

---

## ✅ Итоги

### Текущая архитектура:
- ✅ **3 уровня проверки спама** (быстрая → критическая → полная)
- ✅ **Гибридная классификация** (паттерны + AI)
- ✅ **Компиляция паттернов** для скорости
- ✅ **Порядок приоритета** для ниш
- ✅ **Фильтры исключений** для точности

### Потенциал для улучшения:
- 🔄 **Машинное обучение** для классификации спама
- 🔄 **Морфологический анализ** для поиска синонимов
- 🔄 **Кэширование** для ускорения
- 🔄 **Обратная связь** от пользователей
- 🔄 **Статистика и мониторинг** эффективности
