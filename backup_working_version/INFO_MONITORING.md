# 📋 ИНФОРМАЦИЯ О МОНИТОРИНГЕ

## 🔄 Как работает система мониторинга

### Файл, который обрабатывает сообщения:
**`user_monitor_bot.py`** - это основной файл, который:
1. Подключается к Telegram через Telethon
2. Получает ВСЕ новые сообщения из чатов (`@client.on(events.NewMessage())`)
3. Фильтрует только чаты из списка мониторинга (из `known_chats.json` или `config.py`)
4. Передает сообщения в класс `MessageMonitor` для обработки

### Что ищет система:

Система ищет **НИШИ** (категории) из файла `patterns.py` → `NICHES_KEYWORDS`

#### Процесс обработки сообщения:

1. **ШАГ 1**: Проверка на критический спам (`is_critical_spam_only`)
   - Блокирует: базы данных, наркотики, мошенничество, ассистенты/менеджеры для маркетплейсов

2. **ШАГ 2**: Проверка на дубликаты

3. **ШАГ 3**: Проверка на похожие заблокированные сообщения

4. **ШАГ 4**: Гибридная классификация (`_hybrid_classify_message`)
   - Сначала проверка на спам через `SPAM_FILTERS`
   - Затем поиск ниш через паттерны из `NICHES_KEYWORDS`
   - Если паттерны не нашли ниши → используется AI классификатор

5. **ШАГ 5**: Поиск подписчиков
   - Для каждой найденной ниши система ищет подписчиков через `db.get_subscribers_for_niche(niche)`
   - Отправляет уведомления подписчикам, которые подписаны на эти ниши

### 📂 Где находятся настройки:

- **Паттерны ниш**: `/home/tovgrishkoff/mvp2105/backup_working_version/patterns.py` → `NICHES_KEYWORDS`
- **Спам-фильтры**: `/home/tovgrishkoff/mvp2105/backup_working_version/patterns.py` → `SPAM_FILTERS`
- **Список чатов**: `/home/tovgrishkoff/mvp2105/backup_working_version/known_chats.json` или `config.py` → `MONITORED_CHATS_*`

### 🔍 Что именно ищется:

Система ищет сообщения, которые содержат ключевые слова из `NICHES_KEYWORDS`, например:
- Недвижимость (аренда, продажа, виллы, апартаменты)
- Услуги (фотограф, массаж, ремонт, транспорт)
- Работа (вакансии, поиск работы)
- Мероприятия (вечеринки, концерты, фестивали)
- И другие ниши...

После нахождения ниши система отправляет уведомление всем подписчикам, которые выбрали эту нишу.



