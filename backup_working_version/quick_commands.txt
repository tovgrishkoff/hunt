═══════════════════════════════════════════════════════════════
  КОМАНДЫ ДЛЯ МОНИТОРИНГА БОТА
═══════════════════════════════════════════════════════════════

📊 1. КОЛИЧЕСТВО ПОДПИСЧИКОВ (простая команда):
───────────────────────────────────────────────────────────────
cd /home/tovgrishkoff/mvp2105/backup_working_version && source ../venv/bin/activate && python3 -c "
import asyncio
import asyncpg
from config import DB_DSN

async def count():
    conn = await asyncpg.connect(DB_DSN)
    count = await conn.fetchval('SELECT COUNT(*) FROM subscribers')
    print(f'📊 Всего подписчиков: {count}')
    await conn.close()

asyncio.run(count())
"

───────────────────────────────────────────────────────────────

📊 2. ПОДРОБНАЯ СТАТИСТИКА ПОДПИСЧИКОВ:
───────────────────────────────────────────────────────────────
cd /home/tovgrishkoff/mvp2105/backup_working_version && source ../venv/bin/activate && python3 -c "
import asyncio
import asyncpg
import json
from config import DB_DSN

async def stats():
    conn = await asyncpg.connect(DB_DSN)
    
    # Общее количество
    total = await conn.fetchval('SELECT COUNT(*) FROM subscribers')
    print(f'📊 Всего подписчиков: {total}')
    
    # Подписчики с нишами
    rows = await conn.fetch('SELECT user_id, niches FROM subscribers')
    with_niches = sum(1 for row in rows if row['niches'] and json.loads(row['niches']))
    print(f'📂 С выбранными нишами: {with_niches}')
    
    # Подписчики по странам
    rows = await conn.fetch('SELECT countries FROM subscribers')
    countries = {}
    for row in rows:
        if row['countries']:
            user_countries = json.loads(row['countries'])
            for country in user_countries:
                countries[country] = countries.get(country, 0) + 1
    
    print(f'\\n🌍 Подписчики по странам:')
    for country, count in sorted(countries.items(), key=lambda x: -x[1]):
        print(f'   {country}: {count}')
    
    await conn.close()

asyncio.run(stats())
"

───────────────────────────────────────────────────────────────

📋 3. ЛОГИ В РЕАЛЬНОМ ВРЕМЕНИ (все логи):
───────────────────────────────────────────────────────────────
tail -f /home/tovgrishkoff/mvp2105/backup_working_version/monitor_output.log

───────────────────────────────────────────────────────────────

🚫 4. ЛОГИ С ФИЛЬТРАЦИЕЙ (только спам):
───────────────────────────────────────────────────────────────
tail -f /home/tovgrishkoff/mvp2105/backup_working_version/monitor_output.log | grep --line-buffered -E '(КРИТИЧЕСКИЙ СПАМ|заблокирован|отфильтрован|🚫)'

───────────────────────────────────────────────────────────────

🔍 5. ЛОГИ С ФИЛЬТРАЦИЕЙ (только обработка сообщений):
───────────────────────────────────────────────────────────────
tail -f /home/tovgrishkoff/mvp2105/backup_working_version/monitor_output.log | grep --line-buffered -E '(ШАГ|КЛАССИФИКАЦИЯ|обработк)'

───────────────────────────────────────────────────────────────

📄 6. ПОСЛЕДНИЕ 50 СТРОК ЛОГОВ:
───────────────────────────────────────────────────────────────
tail -n 50 /home/tovgrishkoff/mvp2105/backup_working_version/monitor_output.log

───────────────────────────────────────────────────────────────

📈 7. СТАТИСТИКА ЗАБЛОКИРОВАННОГО СПАМА:
───────────────────────────────────────────────────────────────
tail -n 500 /home/tovgrishkoff/mvp2105/backup_working_version/monitor_output.log | grep -c 'КРИТИЧЕСКИЙ СПАМ' && echo 'заблокировано как критический спам'

───────────────────────────────────────────────────────────────

⚙️ 8. ПРОВЕРКА СТАТУСА ПРОЦЕССА:
───────────────────────────────────────────────────────────────
ps aux | grep '[p]ython3 user_monitor_bot.py'

───────────────────────────────────────────────────────────────

🔄 9. ПЕРЕЗАПУСК МОНИТОРИНГА:
───────────────────────────────────────────────────────────────
cd /home/tovgrishkoff/mvp2105/backup_working_version && kill $(ps aux | grep '[p]ython3 user_monitor_bot.py' | awk '{print $2}') 2>/dev/null; sleep 2; source ../venv/bin/activate && nohup python3 user_monitor_bot.py > monitor_output.log 2>&1 &

───────────────────────────────────────────────────────────────

💡 БЫСТРЫЕ КОМАНДЫ (одной строкой):
───────────────────────────────────────────────────────────────

# Количество подписчиков
cd /home/tovgrishkoff/mvp2105/backup_working_version && source ../venv/bin/activate && python3 -c "import asyncio; import asyncpg; from config import DB_DSN; asyncio.run((lambda: asyncpg.connect(DB_DSN).then(lambda c: c.fetchval('SELECT COUNT(*) FROM subscribers').then(lambda n: print(f'Подписчиков: {n}') and c.close())))())"

# Логи в реальном времени
tail -f /home/tovgrishkoff/mvp2105/backup_working_version/monitor_output.log

# Проверка процесса
ps aux | grep '[p]ython3 user_monitor_bot.py'

═══════════════════════════════════════════════════════════════



