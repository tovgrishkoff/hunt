# ⚠️ Проверка логических конфликтов (КРИТИЧНО!)

## 🛑 Важно: Логические конфликты опаснее технических!

Docker изолирует контейнеры, но они выходят в интернет к одним и тем же серверам Telegram. Если два контейнера используют одну и ту же сессию Telegram - это приведет к банам!

## 📋 Проверка 1: Разделение аккаунтов (КРИТИЧНО!)

### Вопрос: Используют ли старые контейнеры те же сессии, что и новые?

**Старые контейнеры:**
- `lexus-secretary` - использует свои сессии
- `auto-join-ukraine-cars` - использует свои сессии

**Новые контейнеры (проект Ukraine):**
- `ukraine-account-manager` - монтирует `./data/ukraine/sessions/`
- `ukraine-marketer` - монтирует `./data/ukraine/sessions/`
- `ukraine-secretary` - монтирует `./data/ukraine/sessions/`
- `ukraine-activity` - монтирует `./data/ukraine/sessions/`

### ✅ Решение:

1. **Проверить папки сессий:**
   ```bash
   # Старые контейнеры (вероятно используют ./sessions/ или другую папку)
   docker inspect lexus-secretary | grep -A 5 '"Mounts"'
   docker inspect auto-join-ukraine-cars | grep -A 5 '"Mounts"'
   
   # Новые контейнеры используют
   ls -la data/ukraine/sessions/
   ```

2. **Если папки РАЗНЫЕ** → ✅ Безопасно
3. **Если папки ОДИНАКОВЫЕ** → ⚠️ КРИТИЧНО! Нужно разделить аккаунты

### 🔍 Как проверить пересечение аккаунтов:

```bash
# Проверить, какие сессии используются в старой системе
docker exec lexus-secretary ls -1 /app/sessions/ 2>/dev/null | head -10

# Проверить, какие сессии будут использоваться в новой системе
ls -1 data/ukraine/sessions/ 2>/dev/null | head -10

# Если списки пересекаются - КРИТИЧЕСКИЙ КОНФЛИКТ!
```

## 📋 Проверка 2: Конфликт вступаторов

### Проблема:

У вас работают **ДВА вступатора** одновременно:
1. Старый: `auto-join-ukraine-cars`
2. Новый: `ukraine-account-manager` (с `smart_joiner.py`)

### Риск:

Если оба вступатора нацелятся на одну группу с одним аккаунтом:
- Оба попытаются вступить одновременно
- Telegram выдаст FloodWait
- Возможен бан аккаунта

### ✅ Решение: Остановить старый вступатор

```bash
# Остановить старый контейнер
docker stop auto-join-ukraine-cars

# Или отключить автозапуск
docker update --restart=no auto-join-ukraine-cars

# Удалить (опционально, после проверки новой системы)
# docker rm auto-join-ukraine-cars
```

**Почему это безопасно:**
- Новый Smart Joiner умнее и безопаснее
- У него есть обработка FloodWait
- Он использует строгую привязку аккаунта к группе
- Есть warm-up период

## 📋 Проверка 3: Ресурсы сервера (RAM)

### Оценка использования:

- Каждый Python контейнер: ~50-100 МБ RAM
- PostgreSQL: ~100-200 МБ RAM
- Всего новых контейнеров Ukraine: 5 (postgres, account-manager, marketer, secretary, activity)
- Приблизительно: ~300-500 МБ RAM

### Рекомендация:

```bash
# Проверить использование RAM после запуска
htop
# или
free -h
```

Убедитесь, что сервер не уходит в swap.

## ✅ Чек-лист перед запуском

### Критические проверки:

- [ ] **Разделение аккаунтов**: Старые и новые контейнеры используют РАЗНЫЕ папки сессий
- [ ] **Остановлен старый вступатор**: `auto-join-ukraine-cars` остановлен
- [ ] **Проверены ресурсы**: Достаточно RAM на сервере

### Рекомендуемые действия:

1. **Проверить папки сессий:**
   ```bash
   # Проверить, какие папки используют старые контейнеры
   docker inspect lexus-secretary | grep -A 10 '"Mounts"'
   docker inspect auto-join-ukraine-cars | grep -A 10 '"Mounts"'
   ```

2. **Остановить старый вступатор:**
   ```bash
   docker stop auto-join-ukraine-cars
   ```

3. **Запустить новую систему:**
   ```bash
   ./run-ukraine.sh up -d
   ```

4. **Проверить логи (первые 5 минут):**
   ```bash
   ./run-ukraine.sh logs -f
   ```

## 🚨 Если обнаружен конфликт аккаунтов

Если старые и новые контейнеры используют ОДИНАКОВЫЕ сессии:

### Вариант 1: Разделить аккаунты в БД

1. В новой БД (`ukraine_db`) должны быть только аккаунты для Ukraine проекта
2. Старые контейнеры (`lexus-secretary`) должны использовать другие аккаунты
3. Проверить файлы конфигурации аккаунтов:
   - `lexus_accounts_config.json` - для старых контейнеров
   - Аккаунты в БД `ukraine_db` - для новых контейнеров

### Вариант 2: Использовать разные папки сессий

1. Скопировать сессии в `data/ukraine/sessions/` для новых контейнеров
2. Старые контейнеры продолжат использовать свои папки

## 💡 Итоговая рекомендация

**Безопасный порядок действий:**

1. ✅ Проверить разделение аккаунтов (критично!)
2. ✅ Остановить `auto-join-ukraine-cars`
3. ✅ Запустить новую систему: `./run-ukraine.sh up -d`
4. ✅ Следить за логами первые 5-10 минут
5. ✅ Проверить, что аккаунты не получают FloodWait/баны

**Если все проверки пройдены - система готова к запуску!** 🚀
