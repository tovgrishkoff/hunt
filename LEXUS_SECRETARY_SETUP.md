# 📬 Настройка Secretary для Lexus

Secretary для системы Lexus пересылает все личные сообщения на @grishkoff.

## 🎯 Функционал

- ✅ Мониторинг личных сообщений (DM) для всех аккаунтов Lexus
- ✅ Автоматическая пересылка на @grishkoff
- ✅ Информация об отправителе (username, ID, аккаунт)
- ✅ Защита от дублей (не пересылает повторно в течение 60 секунд)
- ✅ Работает без базы данных (только файлы)

## 🚀 Запуск

### Способ 1: Через Docker Compose (рекомендуется)

```bash
docker-compose -f docker-compose.lexus.yml up -d lexus-secretary
```

### Способ 2: Локально (для тестирования)

```bash
python3 lexus_secretary.py
```

## ⚙️ Конфигурация

### Получатель пересылки

По умолчанию сообщения пересылаются на **@grishkoff**.

Чтобы изменить получателя, отредактируйте в `lexus_secretary.py`:

```python
secretary = LexusSecretary(forward_to_username='grishkoff')  # Измените на нужный username
```

### Аккаунты

Secretary автоматически использует те же аккаунты, что и Lexus scheduler:
- Загружает из `accounts_config.json`
- Исключает аккаунты из `ukraine_cars_accounts_config.json` (например, `promotion_alex_ever`)

## 📋 Формат пересылки

При получении сообщения, на @grishkoff будет отправлено:

```
📬 Сообщение для @grishkoff

От: @username (ID: 123456789)
Аккаунт: promotion_account_name
Медиа: ✅ (если есть)
────────────────────────────────────────

[Пересланное оригинальное сообщение]
```

## 🔍 Мониторинг

### Просмотр логов

```bash
# В реальном времени
docker logs -f lexus-secretary

# Последние 50 строк
docker logs --tail=50 lexus-secretary

# Поиск пересылок
docker logs lexus-secretary | grep "Forwarded"
```

### Лог-файл

Логи сохраняются в: `logs/lexus_secretary.log`

## ⚠️ Важные замечания

1. **Права на пересылку:** Убедитесь, что аккаунты могут отправлять сообщения @grishkoff
2. **Защита от спама:** Сообщения не пересылаются повторно в течение 60 секунд (защита от дублей)
3. **Черный список:** Можно добавить `blacklist.txt` для игнорирования определенных пользователей

## 🛠 Устранение проблем

### Ошибка: "Failed to resolve forward target"

**Причина:** Аккаунт не может найти пользователя @grishkoff

**Решение:**
1. Убедитесь, что @grishkoff существует
2. Убедитесь, что хотя бы один аккаунт имеет доступ к этому пользователю
3. Проверьте, что аккаунты авторизованы

### Сообщения не пересылаются

**Проверьте:**
```bash
# Проверьте, что контейнер запущен
docker ps | grep lexus-secretary

# Проверьте логи на ошибки
docker logs lexus-secretary | grep -i "error\|failed"

# Проверьте, что клиенты подключены
docker logs lexus-secretary | grep "✅ Client.*connected"
```

### Слишком много дублей

Увеличьте `recent_window` в коде (по умолчанию 60 секунд):

```python
self.recent_window = 120  # 2 минуты вместо 1
```

## 📊 Статус в мониторинге

Secretary будет показан в `check_status.sh`:

```bash
./check_status.sh
```

---

*Создано: 2026-01-10*
