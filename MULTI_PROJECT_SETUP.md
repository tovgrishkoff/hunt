# 🚀 Настройка мульти-проектной архитектуры

## 📋 Обзор

Система поддерживает запуск двух независимых проектов на одном сервере:
- **Bali** (ниша: `bali_rent`, БД порт: 5438)
- **Ukraine** (ниша: `ukraine_cars`, БД порт: 5439)

Каждый проект использует свой `.env` файл и изолированную БД и данные.

## 📁 Структура проекта

См. `PROJECT_STRUCTURE.md` для детального дерева проекта.

## 🚀 Быстрый старт

### 1. Подготовка структуры папок

```bash
# Создаем структуру папок для данных
mkdir -p data/bali/{postgres,sessions,logs,assets}
mkdir -p data/ukraine/{postgres,sessions,logs,assets}

# Создаем директории для логов (если нужно)
mkdir -p logs
```

### 2. Настройка .env файлов

Убедитесь, что файлы `.env.bali` и `.env.ukraine` существуют и настроены правильно.

### 3. Создание .env файлов

Скопируйте примеры и настройте:

```bash
# Для Bali
cp .env.bali.example .env.bali
# Отредактируйте .env.bali (пароли, API ключи)

# Для Ukraine
cp .env.ukraine.example .env.ukraine
# Отредактируйте .env.ukraine (пароли, API ключи)
```

### 4. Запуск проектов

#### Способ 1: Использование helper скриптов (рекомендуется)

```bash
# Запуск проекта Bali
./run-bali.sh up -d

# Запуск проекта Ukraine
./run-ukraine.sh up -d

# Остановка
./run-bali.sh down
./run-ukraine.sh down

# Просмотр логов
./run-bali.sh logs -f
./run-ukraine.sh logs -f
```

#### Способ 2: Ручной запуск через export

```bash
# Запуск проекта Bali
export $(cat .env.bali | grep -v '^#' | xargs)
docker-compose -f docker-compose.template.yml up -d

# Запуск проекта Ukraine (в другом терминале)
export $(cat .env.ukraine | grep -v '^#' | xargs)
docker-compose -f docker-compose.template.yml up -d
```

#### Одновременный запуск обоих проектов:

```bash
# В отдельных терминалах или через screen/tmux
./run-bali.sh up -d
./run-ukraine.sh up -d
```

### 6. Проверка статуса

```bash
# Статус проекта Bali
./run-bali.sh ps

# Статус проекта Ukraine
./run-ukraine.sh ps

# Все контейнеры
docker ps | grep -E "bali|ukraine"
```

## 📝 Настройка Cron задач

### Способ 1: Автоматическая установка (рекомендуется)

Используйте готовый скрипт для установки:

```bash
./install-cron.sh
```

Скрипт предложит выбрать:
- Только Bali
- Только Ukraine  
- Оба проекта (рекомендуется)

### Способ 2: Ручная установка

#### Вариант A: Использование готовых файлов

```bash
# Для обоих проектов (рекомендуется)
crontab crontab.combined

# Или только для одного проекта
crontab crontab.bali
# или
crontab crontab.ukraine
```

#### Вариант B: Добавление в существующий crontab

```bash
# Открываем редактор
crontab -e

# Добавляем содержимое из crontab.combined (или crontab.bali/crontab.ukraine)
```

### Расписание задач

**Проект Bali:**
- **Разведка (Scout)**: Каждые 6 часов в 00 минут (`0 */6 * * *`)
- **Вступление (Joiner)**: Каждые 2 часа в 15 минут (`15 */2 * * *`)

**Проект Ukraine:**
- **Разведка (Scout)**: Каждые 6 часов в 30 минут (`30 */6 * * *`)
- **Вступление (Joiner)**: Каждые 2 часа в 45 минут (`45 */2 * * *`)

**Зачем такое расписание?**
- Задачи разнесены по времени, чтобы не перегружать систему
- Разведка запускается реже (каждые 6 часов)
- Вступление чаще (каждые 2 часа), чтобы быстро обрабатывать найденные группы

### Просмотр и управление Cron

```bash
# Просмотр всех задач
crontab -l

# Редактирование
crontab -e

# Удаление всех задач
crontab -r

# Проверка работы cron сервиса
sudo systemctl status cron
# или
sudo service cron status
```

### Логи Cron задач

Логи пишутся в:
- **Bali**: `data/bali/logs/scout_cron.log` и `data/bali/logs/joiner_cron.log`
- **Ukraine**: `data/ukraine/logs/scout_cron.log` и `data/ukraine/logs/joiner_cron.log`

```bash
# Просмотр логов
tail -f data/bali/logs/scout_cron.log
tail -f data/bali/logs/joiner_cron.log
tail -f data/ukraine/logs/scout_cron.log
tail -f data/ukraine/logs/joiner_cron.log
```

## 🔧 Полезные команды

### Остановка проектов

```bash
# Остановить Bali
./run-bali.sh down

# Остановить Ukraine
./run-ukraine.sh down
```

### Просмотр логов

```bash
# Логи проекта Bali
./run-bali.sh logs -f

# Логи конкретного сервиса
./run-bali.sh logs -f account-manager

# Логи проекта Ukraine
./run-ukraine.sh logs -f account-manager
```

### Выполнение команд внутри контейнера

```bash
# Запуск scout вручную (Bali)
docker exec -it bali-account-manager python3 services/account-manager/scout.py bali_rent

# Запуск smart_joiner вручную (Ukraine)
docker exec -it ukraine-account-manager python3 services/account-manager/smart_joiner.py ukraine_cars 5

# Заход в контейнер
docker exec -it bali-account-manager bash
```

### Подключение к БД

```bash
# Подключение к БД Bali (порт 5438)
docker exec -it bali-postgres psql -U telegram_user_bali -d bali_db

# Подключение к БД Ukraine (порт 5439)
docker exec -it ukraine-postgres psql -U telegram_user_ukraine -d ukraine_db

# Или с хоста
psql -h localhost -p 5438 -U telegram_user_bali -d bali_db
psql -h localhost -p 5439 -U telegram_user_ukraine -d ukraine_db
```

### Пересборка контейнеров

```bash
# Пересборка после изменения кода
./run-bali.sh build account-manager
./run-bali.sh up -d account-manager

# Или для Ukraine
./run-ukraine.sh build account-manager
./run-ukraine.sh up -d account-manager
```

## 🔄 Workflow: Парсер → БД → Вступатор

1. **Scout (Парсер)** запускается по расписанию (cron)
   - Ищет новые группы
   - Сохраняет в БД со статусом `status='new'`

2. **Smart Joiner (Вступатор)** запускается по расписанию (cron)
   - Выбирает группы со статусом `status='new'`
   - Вступает в группы
   - Обновляет статус на `status='joined'` с привязкой к аккаунту

3. **Marketer (Постер)** использует группы со статусом `status='joined'`
   - Проверяет warm-up период (`warmup_ends_at < NOW()`)
   - Использует привязанный аккаунт (`assigned_account_id`)
   - Делает посты в группы (текст + фото)
   - Обновляет `last_post_at` и записывает в `post_history`
   
   См. `MARKETER_SETUP.md` для подробностей

## ⚠️ Важные замечания

1. **Изоляция данных**: Каждый проект имеет свою БД и папки данных
2. **Общий код**: Код в `services/` общий для всех проектов
3. **Переменные окружения**: Используйте правильный `.env` файл для каждого проекта
4. **Порты**: Убедитесь, что порты 5438 и 5439 свободны на хосте
5. **Hot-reload**: Код в `services/` монтируется как `:ro` (read-only), но изменения применяются без пересборки контейнера

## 🐛 Отладка

### Проверка переменных окружения в контейнере

```bash
docker exec bali-account-manager env | grep -E "PROJECT_NAME|NICHE|DB_"
```

### Проверка подключения к БД

```bash
docker exec bali-account-manager python3 -c "
from lexus_db.session import get_database_url
print(get_database_url())
"
```

### Просмотр структуры БД

```bash
docker exec -it bali-postgres psql -U telegram_user_bali -d bali_db -c "\dt"
```
