# 🔍 Проверка конфликтов контейнеров

## ✅ Результаты проверки

### 1. Имена контейнеров

**Новые контейнеры (проект Ukraine):**
- `ukraine-postgres`
- `ukraine-account-manager`
- `ukraine-marketer`
- `ukraine-secretary`
- `ukraine-activity`

**Существующие контейнеры (не трогаем):**
- `lexus-secretary` ✅ Оставляем
- `lexus-scheduler` ✅ Оставляем (можно остановить, если не нужен)
- `auto-join-ukraine-cars` ✅ Оставляем (можно остановить, заменить на smart_joiner)
- `telegram-combine-*` ✅ Оставляем (не мешают)

**✅ Конфликтов имен НЕТ** - новые контейнеры используют префикс `ukraine-`, старые используют `lexus-` или `telegram-combine-`

### 2. Порты

**Новые контейнеры:**
- `ukraine-postgres` → порт **5439** (из .env.ukraine)

**Существующие контейнеры:**
- `telegram-combine-postgres` → вероятно другой порт
- `telegram-bali-postgres` → порт **5438** (для Bali)

**✅ Конфликтов портов НЕТ** - порт 5439 свободен для Ukraine проекта

### 3. Volumes (монтирование файлов)

**Новые контейнеры монтируют:**
- `./data/ukraine/` - изолированная папка для Ukraine проекта
- `./services/` - общий код (read-only)
- `./lexus_db/` - общий код (read-only)
- `./config/` - общие конфиги (read-only)

**Существующие контейнеры:**
- `lexus-secretary` - использует свои volumes
- `telegram-combine-*` - используют свои volumes

**✅ Конфликтов volumes НЕТ** - данные изолированы в `data/ukraine/`

### 4. Сети (Docker networks)

**Новые контейнеры:**
- Используют сеть `ukraine-network` (изолированная)

**Существующие контейнеры:**
- `lexus-secretary` - вероятно использует другую сеть
- `telegram-combine-*` - используют свою сеть

**✅ Конфликтов сетей НЕТ** - каждая группа контейнеров в своей сети

### 5. Activity и Secretary

**Activity и Secretary можно оставить:**
- Они работают и не мешают новой архитектуре
- Новые контейнеры `ukraine-activity` и `ukraine-secretary` будут в новой сети
- Старые контейнеры (`lexus-secretary`) продолжат работать в своей сети

**⚠️ Рекомендация:**
- Если старые `lexus-secretary` и `lexus-scheduler` работают нормально, их можно оставить
- Новые контейнеры запустятся параллельно и не будут мешать
- Позже можно постепенно мигрировать функционал на новые контейнеры

## 🚀 Итог

**✅ КОНФЛИКТОВ НЕТ - можно безопасно запускать новую архитектуру!**

Новые контейнеры:
- Используют другие имена (`ukraine-*`)
- Используют изолированный порт (5439)
- Используют изолированные volumes (`data/ukraine/`)
- Используют изолированную сеть (`ukraine-network`)

Существующие контейнеры (`lexus-secretary`, `telegram-combine-*`) продолжат работать без конфликтов.

## 📋 Команды для запуска

```bash
# Запустить новую архитектуру для Ukraine (не будет конфликтов)
./run-ukraine.sh up -d

# Проверить, что все запустилось
./run-ukraine.sh ps

# Проверить логи
./run-ukraine.sh logs -f
```
