# ✅ Чек-лист миграции проекта Ukraine/Lexus

## 📋 Пошаговый план действий

### ✅ Шаг 1: Остановить старый вступатор

```bash
# Остановить контейнер
docker stop auto-join-ukraine-cars

# Отключить автозапуск (чтобы не воскрес при перезагрузке)
docker update --restart=no auto-join-ukraine-cars

# Проверить статус
docker ps -a | grep auto-join-ukraine-cars
```

**Ожидаемый результат:** Контейнер остановлен и не запускается автоматически.

---

### ✅ Шаг 2: Подготовить структуру папок

```bash
# Создать папки для данных проекта Ukraine
mkdir -p data/ukraine/{sessions,logs,assets,postgres}

# Проверить структуру
ls -la data/ukraine/
```

**Ожидаемый результат:** Структура папок создана.

---

### ✅ Шаг 3: Скопировать сессии

**Важно:** Нужно решить, какие аккаунты использовать для Ukraine проекта.

#### Вариант А: Использовать новые/отдельные аккаунты

```bash
# Если у вас есть отдельные аккаунты для Ukraine
cp ./sessions/ukraine_account_*.session ./data/ukraine/sessions/ 2>/dev/null || echo "Нет отдельных Ukraine аккаунтов"
```

#### Вариант Б: Перенести аккаунты из старой системы

```bash
# Создать папку для отключенных (бэкап)
mkdir -p ./sessions/disabled

# СКОПИРОВАТЬ сессии в новую папку (НЕ перемещать, сначала копируем!)
# Замените account_name на реальные имена ваших аккаунтов
cp ./sessions/account_name.session ./data/ukraine/sessions/

# ПЕРЕМЕСТИТЬ старые сессии (чтобы старые контейнеры их не использовали)
mv ./sessions/account_name.session ./sessions/disabled/

# Перезапустить старый контейнер (чтобы он "забыл" эти аккаунты)
docker restart lexus-secretary
```

**Ожидаемый результат:** Сессии скопированы в `data/ukraine/sessions/`, старые сессии перемещены в `sessions/disabled/`.

---

### ✅ Шаг 4: Проверить/создать .env.ukraine

```bash
# Проверить наличие файла
test -f .env.ukraine && echo "✅ Файл существует" || echo "❌ Нужно создать"

# Если не существует, создать из примера
if [ ! -f .env.ukraine ]; then
    cp .env.ukraine.example .env.ukraine
    echo "📝 Отредактируйте .env.ukraine (пароли, API ключи)"
    # Открыть для редактирования (опционально)
    # nano .env.ukraine
fi
```

**Ожидаемый результат:** Файл `.env.ukraine` существует и настроен.

---

### ✅ Шаг 5: Запустить миграцию аккаунтов (если нужно)

**Вариант 1: Использовать скрипт миграции (если аккаунты в JSON)**

```bash
# После запуска контейнеров (Шаг 6), запустить миграцию
docker exec -it ukraine-account-manager python3 lexus_db/migrate_from_files.py
```

**Вариант 2: Аккаунты уже в БД или будут добавлены автоматически**

Если аккаунты уже мигрированы или система автоматически подхватит сессии - пропустить этот шаг.

**Ожидаемый результат:** Аккаунты зарегистрированы в БД `ukraine_db`.

---

### ✅ Шаг 6: Запустить новую систему

```bash
# Запустить контейнеры
./run-ukraine.sh up -d

# Проверить статус
./run-ukraine.sh ps

# Дождаться готовности (особенно postgres)
sleep 5
docker logs ukraine-postgres | tail -10
```

**Ожидаемый результат:** Все контейнеры запущены и работают (status: Up).

---

### ✅ Шаг 7: Проверить логи

```bash
# Проверить логи account-manager (важно!)
./run-ukraine.sh logs account-manager | tail -50

# Или в реальном времени
./run-ukraine.sh logs -f account-manager
```

**Что искать:**
- ✅ `Connected` - аккаунты успешно подключены
- ✅ `Database initialized` - БД инициализирована
- ❌ `401 Unauthorized` - аккаунт не авторизован (конфликт!)
- ❌ `SessionRevoked` - Telegram сбросил сессию (конфликт!)

**Ожидаемый результат:** Нет ошибок 401/SessionRevoked, аккаунты подключены.

---

### ✅ Шаг 8: Проверить аккаунты в БД

```bash
# Проверить аккаунты в БД
docker exec -it ukraine-postgres psql -U telegram_user_ukraine -d ukraine_db -c "
SELECT 
    session_name, 
    status, 
    daily_posts_count,
    created_at
FROM accounts 
ORDER BY session_name;
"
```

**Ожидаемый результат:** Аккаунты присутствуют в БД со статусом 'active'.

---

### ✅ Шаг 9: Установить Cron задачи

```bash
# Интерактивная установка (рекомендуется)
./install-cron.sh

# Или вручную
crontab crontab.ukraine
# или для обоих проектов
crontab crontab.combined
```

**Ожидаемый результат:** Cron задачи установлены, можно проверить через `crontab -l`.

---

### ✅ Шаг 10: Финальная проверка

```bash
# Проверить, что все работает
docker ps | grep ukraine

# Проверить логи всех сервисов
./run-ukraine.sh logs --tail=20

# Проверить Cron задачи
crontab -l | grep ukraine
```

---

## 📊 Статус выполнения

- [ ] Шаг 1: Остановить auto-join-ukraine-cars
- [ ] Шаг 2: Подготовить структуру папок
- [ ] Шаг 3: Скопировать сессии
- [ ] Шаг 4: Проверить/создать .env.ukraine
- [ ] Шаг 5: Запустить миграцию аккаунтов (если нужно)
- [ ] Шаг 6: Запустить новую систему
- [ ] Шаг 7: Проверить логи
- [ ] Шаг 8: Проверить аккаунты в БД
- [ ] Шаг 9: Установить Cron задачи
- [ ] Шаг 10: Финальная проверка

## ⚠️ Важные замечания

1. **Разделение аккаунтов:** Убедитесь, что один аккаунт НЕ используется одновременно в старых и новых контейнерах.

2. **Проверка логов:** После запуска обязательно проверьте логи - ошибки 401/SessionRevoked означают конфликт аккаунтов.

3. **Activity и Secretary:** Старые контейнеры (`lexus-secretary`) можно оставить - они не мешают новой системе.

4. **Резервное копирование:** Перед миграцией сделайте бэкап важных данных.

## 🎉 После успешной миграции

Если все шаги выполнены и проверки пройдены:

- ✅ Система готова к работе
- ✅ Аккаунты изолированы
- ✅ Новый Smart Joiner заменил старый
- ✅ Cron задачи настроены
- ✅ Основной цикл работает: Scout → Joiner → Marketer

**Поздравляем! Миграция завершена!** 🚀
