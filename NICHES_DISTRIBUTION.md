# 📊 Распределение сообщений по нишам в системе Bali

## 🔍 Текущее состояние

### Ниши в базе данных

| Ниша | Групп в БД | Постов за сегодня |
|------|------------|-------------------|
| `bali` | 215 | 3 |
| `general` | 62 | 0 |

**Всего:** 277 групп

### Как работает система рассылки

#### 1. Загрузка сообщений

Система **Bali Marketer** загружает сообщения **единым списком** из конфигурации:

```python
# В services/marketer/poster.py
messages = self.config_loader.load_messages()
```

Загружается **один набор сообщений** для всей ниши `bali`, независимо от подкатегорий групп.

#### 2. Выбор групп для рассылки

Система фильтрует группы по нише:

```python
# В get_available_groups()
query = db.query(Group).filter(
    Group.niche == niche,  # niche = 'bali' (активная ниша)
    Group.status == 'active',
    Group.can_post == True,
    # ...
)
```

**Все группы с нишей `bali` получают сообщения из одного и того же пула.**

#### 3. Отсутствие разделения по под-нишам

❌ **Нет разделения** сообщений внутри ниши `bali`:
- Нет разных сообщений для недвижимости / аренды / услуг
- Все группы получают случайные сообщения из общего списка
- Не используется `niche_messages` словарь (как в старой системе `promotion_system.py`)

### 📝 Примеры отправленных сообщений

За сегодня отправлено 3 поста, все в нишу `bali`:

1. **@baliexchanges** (niche: bali)
   - Текст: "Привет! Кто знает где найти видеографа на Бали? Нужна быстрая съемка"

2. **@rent_in_bali** (niche: bali)
   - Текст: "Всем доброго времени! Ищу фотографа с хорошим обслуживанием. Кто посоветует?"

3. **@real_estate_balii** (niche: bali)
   - Текст: "Ищу дизайнера для фирменного стиля, нужен профессионал"

**Все сообщения из одного пула**, не разделены по тематике групп.

---

## 🆚 Сравнение с другими системами

### Старая система (`promotion_system.py`)

Использовала `niche_messages` словарь:

```python
# Логика выбора сообщений по нише группы
group_niche = self.group_niches.get(target, 'general')
if group_niche in self.niche_messages:
    source_messages = self.niche_messages[group_niche]
```

### Текущая система (`services/marketer/poster.py`)

Использует единый список `self.messages`:

```python
# Все группы получают сообщения из одного списка
message = random.choice(self.messages)
```

---

## 💡 Возможные улучшения

### Если нужны разные сообщения для под-ниш:

1. **Добавить поле под-ниши в БД:**
   - Добавить `sub_niche` поле в таблицу `groups`
   - Например: `real_estate`, `rental`, `services`, `general`

2. **Загрузить разные сообщения:**
   - Создать `niche_messages` словарь в `Poster`
   - Загружать сообщения для каждой под-ниши отдельно

3. **Использовать под-нишу при выборе сообщения:**
   ```python
   sub_niche = group.sub_niche or 'general'
   if sub_niche in self.niche_messages:
       source_messages = self.niche_messages[sub_niche]
   else:
       source_messages = self.messages  # fallback
   ```

---

## 📈 Статистика по нишам

### Активные группы (status = 'active')

| Ниша | Количество групп |
|------|------------------|
| `general` | 62 |
| `bali` | 11 |

**Примечание:** Разница в количестве групп (215 vs 11) объясняется тем, что многие группы имеют статус отличный от `active`.

---

*Обновлено: 2026-01-10*
