# 📢 Система активных напоминаний

## Описание

Система автоматически отправляет напоминания пользователям, которые не подписались после истечения триального периода (7 дней).

## Особенности

- ✅ **Автоматическая отправка каждые 2 дня** - активнее, чем раньше
- ✅ **4 варианта сообщений** - разнообразие текстов
- ✅ **Отслеживание отправок** - не спамит пользователей
- ✅ **Максимум 10 напоминаний** - ограничение для защиты от спама
- ✅ **Только пользователи с истекшими триалами** - минимум 7 дней после истечения

## Настройка

### Интервал отправки

По умолчанию: **каждые 2 дня**

Изменить можно в файле `active_reminders.py`:
```python
REMINDER_INTERVAL_DAYS = 2  # Измените на нужное значение (например, 1 для ежедневной отправки)
```

### Максимальное количество напоминаний

По умолчанию: **10 напоминаний на пользователя**

Изменить можно в функции `should_send_reminder()`:
```python
if reminder_count < 10:  # Измените на нужное значение
```

## Запуск

### Вариант 1: Ручной запуск (одноразово)

```bash
cd /home/tovgrishkoff/mvp2105
python3 active_reminders.py
```

### Вариант 2: Фоновый сервис (постоянно)

```bash
cd /home/tovgrishkoff/mvp2105
python3 start_active_reminders.py
```

### Вариант 3: Docker контейнер (рекомендуется)

```bash
cd /home/tovgrishkoff/mvp2105

# Пересобрать и запустить
docker-compose build active-reminders
docker-compose up -d active-reminders

# Просмотр логов
docker-compose logs -f active-reminders
```

## Расписание

- **Проверка**: каждые 6 часов
- **Отправка напоминаний**: каждые 2 дня пользователю (если прошло достаточно времени)

## Логи

- `active_reminders.log` - логи одноразовых запусков
- `active_reminders_service.log` - логи сервиса (при постоянной работе)

## Статистика

Просмотр статистики отправленных напоминаний:

```bash
docker exec bali-postgres psql -U grishkoff -d bali_bot -c "
SELECT 
    user_id,
    last_reminder_sent,
    reminder_count,
    EXTRACT(DAY FROM (NOW() - last_reminder_sent)) as days_since_last
FROM reminder_tracking
ORDER BY reminder_count DESC, last_reminder_sent DESC;
"
```

## Отключение напоминаний для конкретного пользователя

```sql
-- Удалить из отслеживания
DELETE FROM reminder_tracking WHERE user_id = USER_ID;

-- Или установить большое количество отправленных напоминаний
UPDATE reminder_tracking SET reminder_count = 999 WHERE user_id = USER_ID;
```

## Мониторинг

Проверка статуса контейнера:

```bash
docker ps | grep active-reminders
docker logs bali-active-reminders --tail 50
```

## Настройка текстов напоминаний

Отредактируйте массив `REMINDER_MESSAGES` в файле `active_reminders.py` для изменения текстов.





