# 🔀 Миграция аккаунтов - Пошаговое руководство

## 📋 Обзор

При запуске новой архитектуры для проекта Ukraine/Lexus нужно правильно разделить аккаунты и зарегистрировать их в новой БД.

## 🛑 Шаг 1: Остановить старый вступатор

```bash
# Остановить контейнер
docker stop auto-join-ukraine-cars

# Отключить автозапуск (чтобы не воскрес при перезагрузке)
docker update --restart=no auto-join-ukraine-cars
```

**Почему это важно:** Новый `smart_joiner.py` заменит старый вступатор. Два вступатора не должны работать одновременно.

## 🔀 Шаг 2: Разделение аккаунтов

### Сценарий А: У вас есть НОВЫЕ аккаунты для Ukraine

Если аккаунты для Ukraine проекта уже отделены:

```bash
# Создать папку для сессий (если не существует)
mkdir -p data/ukraine/sessions

# Скопировать сессии в новую папку
cp ./sessions/ukraine_account_*.session ./data/ukraine/sessions/
```

✅ **Рисков нет** - аккаунты не пересекаются со старыми контейнерами.

### Сценарий Б: Перенос СТАРЫХ аккаунтов в новый проект

Если нужно перенести аккаунты от `lexus-secretary` в новый проект:

```bash
# 1. Создать папку для сессий
mkdir -p data/ukraine/sessions

# 2. Создать папку для отключенных аккаунтов (бэкап)
mkdir -p ./sessions/disabled

# 3. Скопировать сессии в новую папку
# (Замените account_name на реальные имена ваших аккаунтов)
cp ./sessions/account_name.session ./data/ukraine/sessions/

# 4. Переместить старые сессии в disabled (чтобы старые контейнеры их не использовали)
mv ./sessions/account_name.session ./sessions/disabled/

# 5. Перезапустить старый контейнер (чтобы он "забыл" эти аккаунты)
docker restart lexus-secretary
```

⚠️ **Важно:** Убедитесь, что один и тот же аккаунт НЕ используется одновременно в старых и новых контейнерах!

## 💾 Шаг 3: Регистрация аккаунтов в новой БД

После копирования сессий нужно зарегистрировать аккаунты в новой БД (`ukraine_db`).

### Вариант 1: Использовать миграцию из файлов (если аккаунты в JSON)

Если у вас есть файл `lexus_accounts_config.json` или `ukraine_accounts_config.json`:

```bash
# Запустить миграцию (после запуска контейнеров)
docker exec -it ukraine-postgres psql -U telegram_user_ukraine -d ukraine_db -c "SELECT COUNT(*) FROM accounts;"

# Если таблица пуста, запустить миграцию
docker exec -it ukraine-account-manager python3 -c "
import asyncio
from lexus_db.session import AsyncSessionLocal, init_db
from lexus_db.migrate_from_files import migrate_accounts
import json

async def main():
    await init_db()
    async with AsyncSessionLocal() as session:
        with open('lexus_accounts_config.json', 'r') as f:
            accounts_config = json.load(f)
        # Фильтр для Ukraine аккаунтов (если нужен)
        await migrate_accounts(session, accounts_config, set())

asyncio.run(main())
"
```

### Вариант 2: Ручное добавление аккаунтов (рекомендуется)

Если аккаунты уже в базе через миграцию, или нужно добавить вручную:

1. **Проверить, какие аккаунты есть в конфиге:**
   ```bash
   # Посмотреть аккаунты в конфиге
   cat lexus_accounts_config.json | jq '.[] | .session_name'
   ```

2. **Запустить новую систему** (она подхватит аккаунты из конфига при первой инициализации):
   ```bash
   ./run-ukraine.sh up -d
   ```

3. **Проверить, что аккаунты загружены:**
   ```bash
   docker exec -it ukraine-postgres psql -U telegram_user_ukraine -d ukraine_db -c "SELECT session_name, status FROM accounts;"
   ```

### Вариант 3: Автоматическая загрузка при первом запуске

Если `account-manager` при старте автоматически сканирует папку `sessions/` и добавляет аккаунты в БД - просто перезапустите контейнер после копирования сессий:

```bash
# После копирования сессий
docker restart ukraine-account-manager

# Проверить логи
docker logs ukraine-account-manager -f
```

## 🚀 Шаг 4: Финальный запуск

Перед запуском убедитесь:

- [x] Старый вступатор остановлен (`auto-join-ukraine-cars`)
- [x] Файлы сессий разделены (нет дублей в `./sessions/` и `./data/ukraine/sessions/`)
- [x] Аккаунты зарегистрированы в новой БД (или будут загружены при первом запуске)

**Запуск:**

```bash
# Запустить новую систему
./run-ukraine.sh up -d

# Проверить статус
./run-ukraine.sh ps

# Проверить логи
./run-ukraine.sh logs -f
```

## 👀 Проверка после запуска

### 1. Проверить логи account-manager

```bash
./run-ukraine.sh logs -f account-manager
```

**Что искать:**
- ✅ `Connected` - аккаунты успешно подключены
- ❌ `401 Unauthorized` - аккаунт не авторизован (возможно, используется где-то еще)
- ❌ `SessionRevoked` - Telegram сбросил сессию (конфликт с другим контейнером)
- ❌ `FloodWait` - слишком много запросов (нормально при первом запуске)

### 2. Проверить аккаунты в БД

```bash
docker exec -it ukraine-postgres psql -U telegram_user_ukraine -d ukraine_db -c "
SELECT 
    session_name, 
    status, 
    daily_posts_count,
    last_stats_reset
FROM accounts 
ORDER BY session_name;
"
```

### 3. Проверить группы в БД

```bash
docker exec -it ukraine-postgres psql -U telegram_user_ukraine -d ukraine_db -c "
SELECT 
    link, 
    status, 
    niche,
    assigned_account_id,
    warmup_ends_at
FROM targets 
WHERE niche = 'ukraine_cars'
LIMIT 10;
"
```

## ⚠️ Типичные ошибки

### Ошибка: "401 Unauthorized"

**Причина:** Аккаунт используется в другом контейнере (старом `lexus-secretary`).

**Решение:** 
1. Убедитесь, что сессия НЕ находится в `./sessions/` (или перемещена в `disabled/`)
2. Перезапустите старый контейнер: `docker restart lexus-secretary`
3. Перезапустите новый контейнер: `docker restart ukraine-account-manager`

### Ошибка: "SessionRevoked"

**Причина:** Telegram сбросил сессию из-за конфликта (один аккаунт используется в двух местах).

**Решение:**
1. Остановить ВСЕ контейнеры, использующие этот аккаунт
2. Переавторизовать аккаунт (создать новую сессию)
3. Использовать аккаунт только в одном месте

### Ошибка: Аккаунты не появляются в БД

**Причина:** Аккаунты не зарегистрированы в БД.

**Решение:**
1. Проверить, что файлы сессий скопированы в `data/ukraine/sessions/`
2. Запустить миграцию из JSON конфига (если есть)
3. Или добавить аккаунты вручную через SQL/скрипт

## ✅ Итоговый чек-лист

- [ ] Старый вступатор остановлен: `docker stop auto-join-ukraine-cars`
- [ ] Автозапуск отключен: `docker update --restart=no auto-join-ukraine-cars`
- [ ] Сессии скопированы в `data/ukraine/sessions/`
- [ ] Старые сессии перемещены в `sessions/disabled/` (если нужно)
- [ ] Старый контейнер перезапущен: `docker restart lexus-secretary`
- [ ] Новая система запущена: `./run-ukraine.sh up -d`
- [ ] Логи проверены (нет ошибок 401/SessionRevoked)
- [ ] Аккаунты появились в БД
- [ ] Cron задачи установлены: `./install-cron.sh`

## 🎉 После успешной миграции

Если все проверки пройдены:

1. ✅ Система готова к работе
2. ✅ Аккаунты изолированы
3. ✅ Новый Smart Joiner заменил старый
4. ✅ Можно устанавливать Cron задачи

**Поздравляем! Миграция завершена!** 🚀
