# ✅ Настройка Cron - ЗАВЕРШЕНО

## 📦 Созданные файлы

1. **`crontab.bali`** - Cron задачи для проекта Bali
2. **`crontab.ukraine`** - Cron задачи для проекта Ukraine
3. **`crontab.combined`** - Объединенный crontab для обоих проектов
4. **`install-cron.sh`** - Скрипт автоматической установки
5. **`test-manual.sh`** - Скрипт для ручного тестирования
6. **`CRON_SETUP.md`** - Подробная инструкция

## 🚀 Быстрый старт

### 1. Тестирование (перед установкой Cron)

```bash
# Тест обоих проектов
./test-manual.sh both
```

### 2. Установка Cron

```bash
# Автоматическая установка
./install-cron.sh
```

Или вручную:

```bash
# Для обоих проектов
crontab crontab.combined

# Проверка
crontab -l
```

## 📅 Расписание

### Bali
- **Scout**: Каждые 6 часов в 00 минут (`0 */6 * * *`)
- **Joiner**: Каждые 2 часа в 15 минут (`15 */2 * * *`)

### Ukraine
- **Scout**: Каждые 6 часов в 30 минут (`30 */6 * * *`)
- **Joiner**: Каждые 2 часа в 45 минут (`45 */2 * * *`)

## 📊 Логи

Логи пишутся в:
- `data/bali/logs/scout_cron.log`
- `data/bali/logs/joiner_cron.log`
- `data/ukraine/logs/scout_cron.log`
- `data/ukraine/logs/joiner_cron.log`

## ✅ Готово!

После установки система будет автоматически:
1. Искать новые группы (Scout)
2. Вступать в найденные группы (Smart Joiner)

**Следующий шаг**: Настройка Marketer для постинга в группы! 🚀
