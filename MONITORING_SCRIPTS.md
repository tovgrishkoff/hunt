# 📊 СКРИПТЫ МОНИТОРИНГА

Единый пульт управления для мониторинга систем Bali и Lexus.

---

## 🚀 Быстрый старт

### 1. Краткий статус (рекомендуется для ежедневной проверки)

**Способ 1: Запуск из папки проекта**
```bash
cd /home/tovgrishkoff/PIAR/telegram_promotion_system_bali
./check_status.sh
```

**Способ 2: Запуск с полным путем**
```bash
/home/tovgrishkoff/PIAR/telegram_promotion_system_bali/check_status.sh
```

**Способ 3: Если вы уже в папке проекта**
```bash
./check_status.sh
```

> **💡 Совет:** Если скрипт не запускается, убедитесь что вы в правильной папке:
> ```bash
> pwd  # Должно показать: /home/tovgrishkoff/PIAR/telegram_promotion_system_bali
> ```

**Показывает:**
- ✅ Статус всех контейнеров
- 📝 Последние 5 записей из логов каждого сервиса
- ⚠️ Количество ошибок за последний час
- 📊 Быстрая статистика (группы, посты)
- ⏰ Следующие запланированные слоты

**Использование:** Утром для быстрой проверки состояния системы.

---

### 2. Детальный мониторинг (для анализа проблем)

```bash
./check_detailed.sh
```

**Показывает:**
- 🔍 Ключевые события (вступления, посты, ошибки) - последние 20 записей
- 🚨 Критические ошибки за последние 2 часа
- Фильтрация по важным событиям (Joined, Sent, FloodWait, etc.)

**Использование:** При необходимости детального анализа работы системы.

---

### 3. Диагностика: Почему нет постов?

```bash
./check_marketer_reason.sh
```

**Показывает:**
- ⏰ События пробуждения Marketer
- 🚫 Сообщения "No groups available"
- ✅ Успешные посты
- ❌ Ошибки и пропуски
- 💡 Автоматический диагноз проблемы

**Использование:** Когда видите "Posts today: 0" в check_status.sh, но не понимаете почему.

---

### 4. Диагностика: Статус групп в БД

```bash
./check_groups_status.sh
```

**Показывает:**
- 📊 Статистика по статусам групп (active, new, banned, etc.)
- ⏰ Статус warm-up периода
- 🔍 Последние 10 вступлений в группы
- 💡 Автоматический анализ: достаточно ли активных групп

**Использование:** Когда нужно понять, почему Marketer не может постить (нет активных групп).

---

## 📋 Что показывают скрипты

### check_status.sh

```
📊 ACTIVE CONTAINERS STATUS
  ✅ Все контейнеры и их статус

🏝️ BALI: Account Manager
  Последние 5 логов
  Количество ошибок за час

🏝️ BALI: Marketer
  Последние 5 логов
  Количество ошибок за час

🚗 LEXUS: Scheduler
  Последние 5 логов
  Количество ошибок за час

⚠️ ERROR SUMMARY
  Сводка критических ошибок

📊 QUICK STATS
  Количество групп с постами
  Посты за сегодня

⏰ NEXT SCHEDULED SLOTS
  Следующие слоты для каждого сервиса
```

### check_detailed.sh

```
🏝️ BALI: Account Manager - Key Events
  Фильтрация: Joined, FloodWait, Sleeping, ✅, ❌

🏝️ BALI: Marketer - Key Events
  Фильтрация: Sent, Posted, Write forbidden, banned, ✅, ❌

🚗 LEXUS: Scheduler - Key Events
  Фильтрация: Posted, Sent, Failed, ✅, ❌

🚨 CRITICAL ERRORS
  Только критические ошибки за последние 2 часа
```

---

## 🔧 Дополнительные команды

### Просмотр логов в реальном времени

```bash
# Все сервисы
docker-compose logs -f

# Только Account Manager
docker-compose logs -f account-manager

# Только Marketer
docker-compose logs -f marketer

# Только Lexus
docker-compose -f docker-compose.lexus.yml logs -f
```

### Фильтрация по ключевым словам

```bash
# Только успешные посты
docker-compose logs marketer | grep "✅ Sent"

# Только ошибки
docker-compose logs marketer | grep -i "error\|failed"

# Только вступления в группы
docker-compose logs account-manager | grep "Joined\|✅ Вступил"

# Только FloodWait
docker-compose logs account-manager | grep -i "floodwait"
```

---

## ⏰ Расписание слотов

### Bali System (Asia/Jakarta)

**Account Manager (вступление):**
- 05:00 (early_morning_1)
- 07:00 (early_morning_2)
- 09:00 (morning_1) ← **Критический слот**
- 11:00 (morning_2)

**Marketer (постинг):**
- 08:00 (morning)
- 12:00 (noon)
- 15:00 (afternoon)
- 18:00 (evening)

### Lexus System (Europe/Kiev)

**Scheduler (рассылка):**
- 08:00 (morning)
- 12:00 (noon)
- 15:00 (afternoon)
- 18:00 (evening)
- 20:00 (night)

---

## 🎯 Рекомендуемый режим мониторинга

### Утро (проверка после слота 09:00 Jakarta)

```bash
# 1. Быстрая проверка статуса
./check_status.sh

# 2. Если постов нет - выяснить причину
./check_marketer_reason.sh

# 3. Проверить статус групп в БД
./check_groups_status.sh

# 4. Если есть вопросы - детальный анализ
./check_detailed.sh

# 5. Проверка конкретного события Account Manager
docker-compose logs account-manager | grep -E "Joined|FloodWait|✅|❌" | tail -30
```

### Вечер (перед сном)

```bash
# Быстрая проверка, что все работает
./check_status.sh
```

### При проблемах

```bash
# 1. Почему нет постов?
./check_marketer_reason.sh

# 2. Достаточно ли активных групп?
./check_groups_status.sh

# 3. Детальный мониторинг
./check_detailed.sh

# 4. Просмотр всех ошибок
docker-compose logs --since 24h | grep -iE "error|exception|failed" | tail -50

# 5. Проверка конкретного сервиса
docker-compose logs -f --tail=100 account-manager
```

### Типичные проблемы и решения

**Проблема:** "Posts today: 0" в check_status.sh

**Решение:**
```bash
# 1. Проверить причину
./check_marketer_reason.sh

# 2. Если причина "No groups" - проверить БД
./check_groups_status.sh

# 3. Если групп мало - подождать слот Account Manager (09:00)
# После вступления группы пройдут warm-up (24ч) и станут доступны
```

**Проблема:** "No active groups available"

**Решение:**
```bash
# Проверить статус групп
./check_groups_status.sh

# Если много групп со статусом "new" - Account Manager вступит в 09:00
# Если групп нет совсем - Account Manager найдет новые в следующий слот
```

---

## 📝 Примеры вывода

### Успешная работа

```
✅ Running - Up 2 hours
✅ No errors in last hour
✅ Bali: No critical errors
✅ Lexus: No critical errors
Groups with posts: 37
Posts today: 4
Next slot: morning_1 at 2026-01-10 09:00:00 (in 1h 9m)
```

### Обнаружена проблема

```
⚠️ Errors in last hour: 3
🚨 CRITICAL ERRORS:
FloodWait: 7200 секунд - СЛИШКОМ БОЛЬШОЙ
Write forbidden: no send_messages permission
```

---

## 🛠 Устранение проблем

### Если контейнер не запущен

```bash
# Проверить статус
docker-compose ps

# Перезапустить
docker-compose restart account-manager

# Или пересоздать
docker-compose up -d --force-recreate account-manager
```

### Если много ошибок

```bash
# Просмотреть детальные логи
./check_detailed.sh

# Проверить последние ошибки
docker-compose logs --since 1h | grep -i error | tail -20
```

---

*Обновлено: 2026-01-10*
