# 🔄 ПОЛНЫЙ ЦИКЛ АВТОМАТИЧЕСКОЙ РАБОТЫ СИСТЕМЫ

## ✅ Да, всё работает автоматически! Вот полный цикл:

---

## 🔍 **ЭТАП 1: Поиск новых групп**

**Сервис:** `account-manager` (finder.py)

**Как работает:**
- Ищет группы в Telegram по ключевым словам (например, "bali", "bali rent", "bali property")
- Использует Telegram API для поиска публичных групп и каналов
- Сохраняет найденные группы в БД со статусом `new`

**Расписание:** Автоматически по расписанию (настраивается в scheduler.py)

---

## 👥 **ЭТАП 2: Вступление в группы**

**Сервис:** `account-manager` (joiner.py)

**Как работает:**
1. Берет группы со статусом `new` из БД
2. Выбирает свободный аккаунт (который еще не достиг лимита вступлений)
3. Вступает в группу через Telegram API
4. Обновляет в БД:
   - `status` → `active`
   - `assigned_account_id` → ID аккаунта
   - `joined_at` → время вступления
   - `warm_up_until` → время окончания warm-up (joined_at + 24 часа)

**Расписание:** Автоматически по расписанию (после поиска групп)

---

## ⏳ **ЭТАП 3: Warm-up (отлежка)**

**Сервис:** Автоматическая проверка в БД

**Как работает:**
- После вступления группа получает `warm_up_until = joined_at + 24 часа`
- Система постинга проверяет: `warm_up_until < NOW()` перед постингом
- Группы, у которых warm-up еще не закончился, **не получают посты**

**Цель:** Снизить риск бана аккаунта (аккаунт должен "отлежаться" в группе перед активным постингом)

---

## 🎯 **ЭТАП 4: Релевантный выбор сообщений**

**Сервис:** `marketer` (poster.py)

**Как работает:**
1. Система находит группы, готовые для постинга:
   - `status = 'active'`
   - `warm_up_until < NOW()` (warm-up закончен)
   - `assigned_account_id IS NOT NULL` (привязан аккаунт)
   - `daily_posts_count < 2` (не превышен лимит постов в день)

2. Для каждой группы:
   - Берет `username` группы (например, `@bali_rents`)
   - Ищет в `group_niches.json` → находит категорию (например, `rental_property`)
   - Ищет в `messages.json` сообщения с этой категорией
   - **Выбирает случайное сообщение из релевантной категории**

3. Если категория не найдена:
   - Использует **fallback** → все сообщения (650 штук)

**Пример:**
```
Группа: @bali_rents
  ↓
group_niches.json: @bali_rents → rental_property
  ↓
messages.json: rental_property → 15 сообщений
  ↓
Выбирается случайное сообщение из этих 15
```

---

## 📢 **ЭТАП 5: Автоматический постинг**

**Сервис:** `marketer` (poster.py + scheduler.py)

**Как работает:**
1. Планировщик запускает постинг по расписанию
2. Для каждой готовой группы:
   - Загружает аккаунт из БД
   - Создает Telegram клиент
   - Выбирает релевантное сообщение (см. этап 4)
   - Отправляет сообщение в группу
   - Обновляет счетчики:
     - `last_post_at` → текущее время
     - `daily_posts_count` → +1

3. Если ошибка (например, канал):
   - Помечает группу как `read_only`
   - Пропускает её в следующих запусках

**Расписание:** Автоматически по расписанию (например, каждые 2 часа)

---

## 📊 **ПОЛНЫЙ ЦИКЛ В КАРТИНКЕ:**

```
┌─────────────────────────────────────────────────────────────┐
│ 1. ПОИСК ГРУПП (account-manager/finder.py)                  │
│    🔍 Ищет по ключевым словам                                │
│    📝 Сохраняет в БД (status='new')                          │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. ВСТУПЛЕНИЕ (account-manager/joiner.py)                    │
│    👤 Выбирает аккаунт                                       │
│    ✅ Вступает в группу                                      │
│    📝 Обновляет БД:                                          │
│       - status='active'                                      │
│       - assigned_account_id                                  │
│       - joined_at                                            │
│       - warm_up_until = joined_at + 24ч                      │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. WARM-UP (автоматическая проверка)                         │
│    ⏳ Ждет 24 часа                                           │
│    ✅ Проверяет: warm_up_until < NOW()                       │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. РЕЛЕВАНТНЫЙ ВЫБОР (marketer/poster.py)                    │
│    📋 Находит готовые группы                                 │
│    🎯 Для каждой группы:                                     │
│       - username → group_niches.json → категория             │
│       - категория → messages.json → сообщения                │
│       - Выбирает случайное сообщение                         │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. ПОСТИНГ (marketer/poster.py)                              │
│    📢 Отправляет сообщение в группу                          │
│    📝 Обновляет счетчики                                     │
│    ✅ Готово!                                                │
└─────────────────────────────────────────────────────────────┘
```

---

## ⚙️ **АВТОМАТИЗАЦИЯ:**

### Account Manager (поиск + вступление):
- **Расписание:** Настраивается в `scheduler.py`
- **Частота:** Обычно раз в день (ночью)
- **Лимиты:** Максимум вступлений в день на аккаунт

### Marketer (постинг):
- **Расписание:** Настраивается в `scheduler.py`
- **Частота:** Например, каждые 2-4 часа
- **Лимиты:** 
  - Максимум 2 поста в группу в день
  - Максимум 20 постов на аккаунт в день

---

## 🎯 **РЕЗУЛЬТАТ:**

✅ **Полностью автоматический цикл:**
1. Система сама ищет группы
2. Сама вступает в них
3. Сама ждет warm-up
4. Сама выбирает релевантные сообщения
5. Сама постит в группы

**Вы только:**
- Настраиваете расписание
- Добавляете группы в `group_niches.json` (опционально, для релевантности)
- Мониторите результаты

---

## 📊 **ТЕКУЩИЙ СТАТУС:**

✅ **Работает:**
- Поиск групп
- Вступление в группы
- Warm-up отслеживание
- Релевантный выбор сообщений
- Автоматический постинг

✅ **Успешно протестировано:**
- 2 из 3 групп получили посты
- Релевантный выбор работает (`rental_property` → 15 сообщений)
- Каналы автоматически помечаются как `read_only`

---

*Документ создан: 2026-01-13*
