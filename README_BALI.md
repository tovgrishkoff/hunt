# 🏝️ Система "Комбайн" для Бали

**Полностью отдельная система** для постинга про бота @Lead_Hunbot в группы про Бали.

---

## 📋 Что это

Это **копия** системы из `telegram_promotion_system`, но полностью изолированная:
- ✅ Отдельная папка: `/home/tovgrishkoff/PIAR/telegram_promotion_system_bali/`
- ✅ Отдельная БД: `telegram_promotion_bali` (порт 5438)
- ✅ Отдельные контейнеры: `telegram-bali-*`
- ✅ Отдельная сеть Docker: `bali-network`
- ✅ Никаких пересечений с системой для Украины!

---

## 🎯 Цель

**Ненавязчивый постинг** сообщений в группы про Бали:
- Сообщения якобы от человека, который ищет услуги
- Например: "Ищу байк в аренду на Бали..."
- Люди видят это и пишут в личку
- **Secretary** отвечает и консультирует про @Lead_Hunbot

---

## ⚙️ Настройка

### 1. Аккаунты

Добавьте аккаунты для Бали в `sessions/`:
```bash
cd /home/tovgrishkoff/PIAR/telegram_promotion_system_bali
mkdir -p sessions

# Скопировать сессии аккаунтов для Бали из основной системы
cp ../telegram_promotion_system/sessions/promotion_*.session sessions/
```

### 2. Сообщения

Сообщения уже созданы в `bali_assets/messages.json`:
- 15 сообщений про поиск байков в аренду
- Формат: ненавязчивые, якобы человек ищет услуги

**Добавить свои сообщения:**
- Отредактировать `bali_assets/messages.json`
- Формат JSON: `[{"text": "...", "photo": null}, ...]`

### 3. Конфигурация

**Активная ниша:** `config/active_niche.json` → `bali`

**Конфиг ниши:** `config/niches/bali.json`
- Ключевые слова для поиска групп
- Расписание постинга (Asia/Jakarta)
- Настройки секретаря (реклама @Lead_Hunbot)

### 4. Переменные окружения

```bash
cd /home/tovgrishkoff/PIAR/telegram_promotion_system_bali
cp .env.example .env
# Отредактировать .env и добавить OPENAI_API_KEY
```

---

## 🚀 Запуск

```bash
cd /home/tovgrishkoff/PIAR/telegram_promotion_system_bali

# Собрать контейнеры
docker-compose build

# Запустить
docker-compose up -d

# Проверить логи
docker-compose logs -f
```

---

## 📊 Сервисы

1. **Account Manager** (`telegram-bali-account-manager`)
   - Ищет группы про Бали по ключевым словам
   - Вступает в найденные группы
   - Расписание: 05:00, 07:00, 09:00, 11:00 (Asia/Jakarta)

2. **Marketer** (`telegram-bali-marketer`)
   - Постит ненавязчивые сообщения про поиск услуг
   - Расписание: 08:00, 12:00, 15:00, 18:00 (Asia/Jakarta)

3. **Activity** (`telegram-bali-activity`)
   - Просматривает Stories для активности

4. **Secretary** (`telegram-bali-secretary`)
   - Отвечает на личные сообщения
   - Консультирует про @Lead_Hunbot

---

## 🔍 Мониторинг

### Проверить логи:
```bash
docker-compose logs -f marketer
docker-compose logs -f account-manager
docker-compose logs -f secretary
```

### Проверить статистику:
```bash
docker-compose exec -T postgres psql -U telegram_user_bali -d telegram_promotion_bali -c "
SELECT COUNT(*) as total_posts, 
       COUNT(CASE WHEN success = true THEN 1 END) as successful,
       COUNT(CASE WHEN success = false THEN 1 END) as failed
FROM posts 
WHERE DATE(sent_at) = CURRENT_DATE;
"
```

---

## ⚠️ Важно

- **Не пересекается** с системой для Украины
- **Разные БД** - никаких конфликтов
- **Разные порты** - можно запускать одновременно

---

## 📝 Структура

```
telegram_promotion_system_bali/
├── config/
│   ├── active_niche.json      # → bali
│   └── niches/
│       └── bali.json          # Конфиг для Бали
├── bali_assets/
│   └── messages.json          # Сообщения для постинга
├── sessions/                  # Сессии аккаунтов для Бали
├── docker-compose.yml         # Отдельный compose с другой БД
└── ...
```

---

*Система создана: 2026-01-09*
