# 🤖 Bali Bot - Система мониторинга и уведомлений

Telegram-бот для мониторинга сообщений в каналах Бали и уведомления подписчиков по категориям.
. Админ получил все ниши
Пользователь 210147380 имеет доступ ко всем 18 нишам
Для админа сняты ограничения на количество ниш
2. Добавлены админ-команды для управления нишами
/add_niches [user_id] [ниша1] [ниша2] ...
Добавляет ниши пользователю (без удаления существующих)
Пример: /add_niches 418544967 Фотограф Видеограф
/set_niches [user_id] [ниша1] [ниша2] ...
Устанавливает ниши пользователю (заменяет все текущие)
Пример: /set_niches 418544967 Фотограф
/user_niches [user_id]
Показывает текущие ниши пользователя и статус подписки
Пример: /user_niches 418544967
3. Обновлена функция лимитов
Админ (210147380) имеет неограниченный доступ к нишам
Обычные пользователи: 1 ниша в триале, 1 ниша в базовой подписке
Как использовать после оплаты
Пользователь оплачивает подписку
Админ получает уведомление/запрос
Админ выполняет команду:
.
   /add_niches [user_id] [ниша1] [ниша2] ...
Пользователь автоматически получает уведомление о добавленных нишах
Доступные ниши:
Фотограф, Видеограф, Сдача недвижимости, Маникюр, Волосы, Аренда авто, Реснички, Брови, Макияж, Косметология, Продажа недвижимости, Аренда байков, Обмен валют, Кальяны, Аренда Playstation, Медиа-студия, Туризм, Транспорт
Система готова к работе.
---

## 🚀 Быстрый старт

### Просмотр подписчиков

Самый простой способ - использовать готовый скрипт:

```bash
/home/tovgrishkoff/mvp2105/view_subscribers.sh
```

Этот скрипт покажет:
- Список всех подписчиков со статусами
- Информацию о триале и подписках
- Общую статистику по боту

### Создание бэкапа базы данных

```bash
/home/tovgrishkoff/mvp2105/backup_db.sh
```

---

## 📚 Документация

### 📊 [README_DATABASE.md](README_DATABASE.md)
**Полное руководство по работе с базой данных**
- Просмотр подписчиков
- Финансовая информация
- Работа с сообщениями
- SQL-запросы для аналитики
- Экспорт данных

### 💾 [BACKUP_INFO.md](BACKUP_INFO.md)
**Все о бэкапах базы данных**
- Автоматическое резервное копирование
- Восстановление из бэкапа
- Устранение проблем
- Мониторинг бэкапов

---

## 🐳 Docker контейнеры

Проект состоит из следующих контейнеров:

```
bali-bot              - Основной Telegram-бот
bali-user-monitor     - Мониторинг пользователей
bali-reminders        - Система напоминаний
bali-postgres         - База данных PostgreSQL 15
```

### Управление контейнерами

```bash
# Статус всех контейнеров
docker ps | grep -E "bali|mvp2105"

# Перезапуск бота
docker restart bali-bot

# Логи бота
docker logs bali-bot --tail 100 -f

# Остановка всех сервисов
docker stop bali-bot bali-user-monitor bali-reminders

# Запуск всех сервисов
docker start bali-bot bali-user-monitor bali-reminders
```

---

## 📁 Структура проекта

```
/home/tovgrishkoff/mvp2105/
├── backup_working_version/     # Рабочая версия кода
│   ├── bot.py                  # Основной файл бота
│   ├── monitor.py              # Модуль мониторинга
│   ├── user_monitor_bot.py     # Бот мониторинга пользователей
│   ├── database.py             # Работа с БД
│   ├── docker-compose.yml      # Docker конфигурация
│   └── ...
├── backups/                    # Бэкапы базы данных
├── logs/                       # Логи приложений
├── data/                       # Данные (кэш, обучение AI)
├── improved_messages/          # Улучшенные сообщения по категориям
├── backup_messages/            # Бэкапы сообщений
│
├── README.md                   # Этот файл
├── README_DATABASE.md          # Руководство по БД
├── BACKUP_INFO.md              # Информация о бэкапах
├── view_subscribers.sh         # Просмотр подписчиков
└── backup_db.sh                # Скрипт бэкапа
```

---

## 🗄️ База данных

**PostgreSQL 15**
- **Контейнер**: `bali-postgres`
- **База данных**: `bali_bot`
- **Пользователь**: `grishkoff`
- **Порт**: `5434` (внешний)

### Таблицы

- `subscribers` - Подписчики бота
- `messages` - Сообщения из каналов
- `payments` - История платежей
- `user_balance` - Балансы и рефералы
- `banned_users` - Заблокированные пользователи
- `referrals` - Реферальная программа

---

## 🔧 Администрирование

### Подключение к базе данных

```bash
docker exec -it bali-postgres psql -U grishkoff -d bali_bot
```

### Быстрые команды

```bash
# Количество подписчиков
docker exec bali-postgres psql -U grishkoff -d bali_bot -c "SELECT COUNT(*) FROM subscribers;"

# Активные подписки
docker exec bali-postgres psql -U grishkoff -d bali_bot -c "SELECT COUNT(*) FROM subscribers WHERE subscription_active = true;"

# Активные триалы
docker exec bali-postgres psql -U grishkoff -d bali_bot -c "SELECT COUNT(*) FROM subscribers WHERE trial_until > NOW();"
```

---

## 💾 Автоматический бэкап

**Статус**: ✅ Настроен и работает

- **Расписание**: Ежедневно в 03:00
- **Директория**: `/home/tovgrishkoff/mvp2105/backups/`
- **Срок хранения**: 30 дней

### Проверка последнего бэкапа

```bash
ls -lht /home/tovgrishkoff/mvp2105/backups/*.sql.gz | head -1
```

### Просмотр логов бэкапа

```bash
tail -50 /home/tovgrishkoff/mvp2105/backups/backup.log
```

---

## 📊 Мониторинг

### Логи приложений

```bash
# Бот
docker logs bali-bot --tail 100 -f

# Мониторинг пользователей
docker logs bali-user-monitor --tail 100 -f

# База данных
docker logs bali-postgres --tail 100 -f
```

### Статус системы

```bash
# Использование ресурсов контейнерами
docker stats bali-bot bali-user-monitor bali-postgres

# Место на диске
df -h
```

---

## 🔐 Безопасность

- **Администратор**: User ID `210147380` (whitelisted, не может быть заблокирован)
- **Переменные окружения**: `.env` и `.env.monitor`
- **Пароли**: Хранятся в переменных окружения

---

## 🆘 Помощь

### Если бот не работает

1. Проверить статус контейнера:
   ```bash
   docker ps | grep bali-bot
   ```

2. Посмотреть логи:
   ```bash
   docker logs bali-bot --tail 50
   ```

3. Перезапустить:
   ```bash
   docker restart bali-bot
   ```

### Если бэкап не работает

1. Проверить существование скрипта:
   ```bash
   ls -l /home/tovgrishkoff/mvp2105/backup_db.sh
   ```

2. Проверить права:
   ```bash
   chmod +x /home/tovgrishkoff/mvp2105/backup_db.sh
   ```

3. Запустить вручную:
   ```bash
   /home/tovgrishkoff/mvp2105/backup_db.sh
   ```

### Если база данных не отвечает

1. Проверить контейнер PostgreSQL:
   ```bash
   docker ps | grep postgres
   ```

2. Перезапустить базу данных:
   ```bash
   docker restart bali-postgres
   ```

3. Проверить подключение:
   ```bash
   docker exec bali-postgres pg_isready -U grishkoff
   ```

---

## 📝 Примечания

- Проект использует **aiogram 2.x** для работы с Telegram Bot API
- База данных работает на **PostgreSQL 15** с поддержкой JSONB
- Все время в базе данных хранится в **UTC**
- Триальный период составляет **7 дней** с момента регистрации
- Автоматический бэкап выполняется **ежедневно в 03:00**

---

## 🔗 Полезные ссылки

- [Документация по базе данных](README_DATABASE.md)
- [Информация о бэкапах](BACKUP_INFO.md)
- [Aiogram Documentation](https://docs.aiogram.dev/en/latest/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

---

**Создано**: 28 октября 2025  
**Версия проекта**: MVP 2105  
**Локация**: Бали, Индонезия 🌴






