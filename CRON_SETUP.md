# 📅 Настройка Cron для автоматизации

## 🎯 Цель

Настроить автоматический запуск скриптов `scout.py` (разведка) и `smart_joiner.py` (вступление) для обоих проектов.

## 🚀 Быстрая установка

```bash
# Автоматическая установка (рекомендуется)
./install-cron.sh
```

Скрипт предложит выбрать проект и установит задачи автоматически.

## 📋 Расписание задач

### Проект Bali

| Задача | Расписание | Описание |
|--------|-----------|----------|
| Scout (Разведка) | `0 */6 * * *` | Каждые 6 часов в 00 минут |
| Smart Joiner (Вступление) | `15 */2 * * *` | Каждые 2 часа в 15 минут |

### Проект Ukraine

| Задача | Расписание | Описание |
|--------|-----------|----------|
| Scout (Разведка) | `30 */6 * * *` | Каждые 6 часов в 30 минут |
| Smart Joiner (Вступление) | `45 */2 * * *` | Каждые 2 часа в 45 минут |

## 📝 Ручная установка

### Шаг 1: Проверка контейнеров

Убедитесь, что контейнеры запущены:

```bash
docker ps | grep -E "bali-account-manager|ukraine-account-manager"
```

Если контейнеры не запущены:

```bash
./run-bali.sh up -d
./run-ukraine.sh up -d
```

### Шаг 2: Создание директорий для логов

```bash
mkdir -p data/bali/logs
mkdir -p data/ukraine/logs
```

### Шаг 3: Установка Cron задач

#### Вариант A: Использование готовых файлов

```bash
# Для обоих проектов
crontab crontab.combined

# Или только для одного
crontab crontab.bali
crontab crontab.ukraine
```

#### Вариант B: Редактирование вручную

```bash
# Открываем редактор
crontab -e

# Добавляем задачи (скопируйте содержимое из crontab.combined)
```

#### Вариант C: Добавление к существующим задачам

```bash
# Добавляем к существующим (не перезаписываем)
(crontab -l 2>/dev/null; cat crontab.combined) | crontab -
```

## ✅ Проверка установки

```bash
# Просмотр всех cron задач
crontab -l

# Должны быть видны задачи с docker exec
crontab -l | grep -E "bali|ukraine"
```

## 🧪 Ручное тестирование (перед настройкой Cron)

Перед установкой cron, протестируйте скрипты вручную:

### Тест проекта Bali

```bash
# Тест разведки
docker exec bali-account-manager python3 /app/services/account-manager/scout.py bali_rent

# Тест вступления (малый батч для теста)
docker exec bali-account-manager python3 /app/services/account-manager/smart_joiner.py bali_rent 2
```

### Тест проекта Ukraine

```bash
# Тест разведки
docker exec ukraine-account-manager python3 /app/services/account-manager/scout.py ukraine_cars

# Тест вступления
docker exec ukraine-account-manager python3 /app/services/account-manager/smart_joiner.py ukraine_cars 2
```

## 📊 Мониторинг логов

### Просмотр логов Cron

```bash
# Логи разведки Bali
tail -f data/bali/logs/scout_cron.log

# Логи вступления Bali
tail -f data/bali/logs/joiner_cron.log

# Логи разведки Ukraine
tail -f data/ukraine/logs/scout_cron.log

# Логи вступления Ukraine
tail -f data/ukraine/logs/joiner_cron.log
```

### Просмотр системных логов Cron

```bash
# Ubuntu/Debian
sudo tail -f /var/log/syslog | grep CRON

# CentOS/RHEL
sudo tail -f /var/log/cron
```

## 🔧 Управление Cron задачами

### Просмотр задач

```bash
crontab -l
```

### Редактирование задач

```bash
crontab -e
```

### Удаление всех задач

```bash
crontab -r
```

### Проверка статуса сервиса Cron

```bash
# Ubuntu/Debian
sudo systemctl status cron

# CentOS/RHEL
sudo systemctl status crond

# Старые системы
sudo service cron status
```

## ⚠️ Важные замечания

1. **Пути в cron файлах**: Убедитесь, что пути в `crontab.*` файлах соответствуют вашему окружению
   - Путь к директории проекта: `/home/tovgrishkoff/PIAR/telegram_promotion_system_bali`
   - При необходимости отредактируйте пути в файлах `crontab.*`

2. **Права доступа**: Убедитесь, что пользователь имеет права на выполнение `docker exec`

3. **Логи**: Логи пишутся в `data/{project}/logs/`. Убедитесь, что директории созданы и доступны для записи

4. **Время выполнения**: Задачи разнесены по времени, чтобы не перегружать систему

## 🐛 Отладка

### Задача не выполняется

1. Проверьте, что контейнер запущен:
   ```bash
   docker ps | grep account-manager
   ```

2. Проверьте логи cron:
   ```bash
   tail -f data/bali/logs/scout_cron.log
   ```

3. Проверьте системные логи:
   ```bash
   sudo tail -f /var/log/syslog | grep CRON
   ```

4. Попробуйте выполнить команду вручную:
   ```bash
   docker exec bali-account-manager python3 /app/services/account-manager/scout.py bali_rent
   ```

### Проблемы с путями

Если cron не может найти файлы, проверьте:
- Абсолютные пути в cron файлах
- Существование директорий для логов
- Права доступа к файлам и директориям

## 📚 Дополнительная информация

- [Cron формат времени](https://crontab.guru/)
- Расписание задач разнесено для балансировки нагрузки
- Разведка запускается реже (каждые 6 часов), так как поиск новых групп не требует высокой частоты
- Вступление запускается чаще (каждые 2 часа) для быстрой обработки найденных групп
