# 📁 Структура проекта для мульти-проектной архитектуры

## Дерево проекта

```
telegram_promotion_system_bali/
├── docker-compose.yml              # Универсальный шаблон (использует .env)
├── .env.bali                       # Конфигурация для проекта Bali
├── .env.ukraine                    # Конфигурация для проекта Ukraine
│
├── services/                       # Общий код сервисов (используется обоими проектами)
│   ├── account-manager/
│   │   ├── Dockerfile
│   │   ├── smart_joiner.py         # Вступатор (готов)
│   │   ├── scout.py                # Парсер/разведчик (шаблон)
│   │   └── main.py
│   ├── marketer/
│   │   ├── Dockerfile
│   │   ├── poster.py
│   │   └── scheduler.py
│   ├── secretary/
│   │   ├── Dockerfile
│   │   ├── responder.py
│   │   └── gpt_handler.py
│   └── activity/
│       ├── Dockerfile
│       └── story_viewer.py
│
├── shared/                         # Общие модули
│   ├── database/
│   └── config/
│
├── lexus_db/                       # Модули БД для Lexus (ukraine)
│   ├── models.py
│   ├── db_manager.py
│   └── session.py
│
├── data/                           # Данные проектов (разделены)
│   ├── bali/
│   │   ├── postgres/               # Данные БД
│   │   ├── sessions/               # Telegram сессии
│   │   ├── logs/                   # Логи
│   │   └── assets/                 # Медиа файлы
│   └── ukraine/
│       ├── postgres/
│       ├── sessions/
│       ├── logs/
│       └── assets/
│
├── config/                         # Конфигурационные файлы (общие)
│   ├── bali.json
│   └── cars.json
│
├── .env.bali                       # ← Загружается для проекта Bali
├── .env.ukraine                    # ← Загружается для проекта Ukraine
│
└── docker-compose.yml              # Универсальный шаблон
```

## Разделение данных

### Bali проект:
- **БД**: `data/bali/postgres/`
- **Сессии**: `data/bali/sessions/`
- **Логи**: `data/bali/logs/`
- **Медиа**: `data/bali/assets/`

### Ukraine проект:
- **БД**: `data/ukraine/postgres/`
- **Сессии**: `data/ukraine/sessions/`
- **Логи**: `data/ukraine/logs/`
- **Медиа**: `data/ukraine/assets/`

## Переменные окружения

Каждый проект использует свой `.env` файл:
- `.env.bali` → `PROJECT_NAME=bali`, `DB_PORT=5438`, `NICHE=bali_rent`
- `.env.ukraine` → `PROJECT_NAME=ukraine`, `DB_PORT=5439`, `NICHE=ukraine_cars`

## Общие компоненты

- **Код сервисов**: `services/` (общий для всех проектов)
- **Конфигурации ниш**: `config/` (общие JSON файлы)
- **Модули БД**: `lexus_db/`, `shared/` (общие)
