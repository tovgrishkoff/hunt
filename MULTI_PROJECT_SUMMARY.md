# ✅ Мульти-проектная архитектура - Резюме

## 📦 Созданные файлы

1. **`docker-compose.template.yml`** - Универсальный шаблон compose файла
2. **`.env.bali.example`** - Пример конфигурации для проекта Bali
3. **`.env.ukraine.example`** - Пример конфигурации для проекта Ukraine
4. **`run-bali.sh`** - Helper скрипт для запуска проекта Bali
5. **`run-ukraine.sh`** - Helper скрипт для запуска проекта Ukraine
6. **`services/account-manager/scout.py`** - Шаблон парсера (разведчика)
7. **`PROJECT_STRUCTURE.md`** - Структура проекта
8. **`MULTI_PROJECT_SETUP.md`** - Полная инструкция по настройке

## 🎯 Ключевые особенности

### Разделение данных
- **Bali**: `data/bali/` (postgres, sessions, logs, assets)
- **Ukraine**: `data/ukraine/` (postgres, sessions, logs, assets)

### Изоляция проектов
- Разные БД (порты 5438 и 5439)
- Разные сети Docker
- Разные контейнеры (префикс `${PROJECT_NAME}`)

### Общий код
- `services/` - общий для всех проектов
- `config/` - общие конфигурации
- `shared/` - общие модули
- `lexus_db/` - модули БД

### Переменные окружения
- `${PROJECT_NAME}` - имя проекта (bali/ukraine)
- `${NICHE}` - ниша (bali_rent/ukraine_cars)
- `${DB_NAME}`, `${DB_USER}`, `${DB_PASSWORD}`, `${DB_PORT}`

## 🚀 Быстрый старт

```bash
# 1. Создать .env файлы
cp .env.bali.example .env.bali
cp .env.ukraine.example .env.ukraine

# 2. Настроить пароли и ключи в .env файлах

# 3. Создать структуру папок
mkdir -p data/{bali,ukraine}/{postgres,sessions,logs,assets}

# 4. Запустить проекты
./run-bali.sh up -d
./run-ukraine.sh up -d
```

## 📝 Cron задачи

### Внутри контейнера account-manager:

```cron
# Bali - Разведка (каждые 6 часов)
0 */6 * * * cd /app && python3 services/account-manager/scout.py bali_rent >> /app/logs/scout_cron.log 2>&1

# Bali - Вступление (каждые 2 часа)
0 */2 * * * cd /app && python3 services/account-manager/smart_joiner.py bali_rent 5 >> /app/logs/joiner_cron.log 2>&1

# Ukraine - Разведка (каждые 6 часов)
0 */6 * * * cd /app && python3 services/account-manager/scout.py ukraine_cars >> /app/logs/scout_cron.log 2>&1

# Ukraine - Вступление (каждые 2 часа)
0 */2 * * * cd /app && python3 services/account-manager/smart_joiner.py ukraine_cars 5 >> /app/logs/joiner_cron.log 2>&1
```

## 🔄 Workflow

1. **Scout (Парсер)** → находит группы → сохраняет в БД (`status='new'`)
2. **Smart Joiner (Вступатор)** → выбирает группы (`status='new'`) → вступает → обновляет (`status='joined'`)
3. **Marketer (Постер)** → использует группы (`status='joined'`, warm-up завершен) → делает посты

## ✅ Все требования выполнены

- ✅ Универсальный docker-compose.yml с переменными
- ✅ Разделение данных по папкам `data/bali/` и `data/ukraine/`
- ✅ .env файлы для каждого проекта
- ✅ Шаблон парсера `scout.py`
- ✅ Инструкции по запуску и настройке Cron
- ✅ Helper скрипты для удобного запуска
