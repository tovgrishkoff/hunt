# ⚡ БЫСТРЫЙ СТАРТ - УПРАВЛЕНИЕ СИСТЕМОЙ

## 🎯 **ВАША ГЛАВНАЯ ЗАДАЧА:**

### 👉 **Управление через `group_niches.json`**

Система работает автоматически! Вам нужно только:

1. **Раз в 2-3 дня** проверять новые группы в БД
2. **Добавлять их в `group_niches.json`** с правильными категориями
3. **Наслаждаться результатами!** 🎉

---

## 📋 **БЫСТРАЯ ПРОВЕРКА НОВЫХ ГРУПП:**

```bash
# Посмотреть новые группы (последние 20)
docker exec telegram-bali-postgres psql -U telegram_user_bali -d telegram_promotion_bali -c "
SELECT username, title, status, created_at 
FROM groups 
WHERE niche = 'bali' 
  AND status IN ('new', 'active')
ORDER BY created_at DESC 
LIMIT 20;
"
```

---

## ✏️ **ДОБАВЛЕНИЕ ГРУПП В МАППИНГ:**

```bash
# Открыть файл
nano group_niches.json
```

**Добавьте группы в формате:**
```json
{
  "@bali_property_rent": "rental_property",
  "@bali_business_chat": "general",
  "@bali_digital_nomads": "general"
}
```

**Популярные категории:**
- `rental_property` - Аренда недвижимости (15 сообщений)
- `sale_property` - Продажа недвижимости (15 сообщений)
- `general` - Общие группы (15 сообщений)
- `car_rental` - Аренда авто (15 сообщений)
- `bike_rental` - Аренда мотоциклов (15 сообщений)
- `tourism` - Туризм (15 сообщений)
- И другие (см. полный список в `config/messages/bali/messages.json`)

---

## 🔍 **РАЗВЕДКА (Поиск новых групп):**

### ✅ **Автоматически:**
Account Manager уже работает и автоматически ищет группы по расписанию!

### 🔧 **Вручную (если нужно):**

Account Manager использует встроенный поиск через `finder.py`, который работает автоматически по расписанию.

**Проверка работы:**
```bash
# Логи Account Manager
docker logs telegram-bali-account-manager --tail 50

# Статистика групп
docker exec telegram-bali-postgres psql -U telegram_user_bali -d telegram_promotion_bali -c "
SELECT status, COUNT(*) 
FROM groups 
WHERE niche = 'bali' 
GROUP BY status;
"
```

---

## 📊 **МОНИТОРИНГ:**

### Проверка готовых групп:
```bash
docker exec telegram-bali-postgres psql -U telegram_user_bali -d telegram_promotion_bali -c "
SELECT username, status, warm_up_until, last_post_at
FROM groups 
WHERE niche = 'bali' 
  AND status = 'active' 
  AND warm_up_until <= NOW()
ORDER BY last_post_at ASC NULLS FIRST
LIMIT 10;
"
```

### Последние посты:
```bash
docker logs telegram-bali-marketer --tail 30 | grep -E "(✅|❌|📋|🎯)"
```

---

## 🚀 **ЗАПУСК ПОСТИНГА ВРУЧНУЮ:**

```bash
# Запустить постинг (5 групп)
docker exec telegram-bali-marketer python3 /app/services/marketer/poster.py bali 5
```

---

## 📝 **ПРИМЕРЫ ДОБАВЛЕНИЯ:**

### Недвижимость:
```json
"@bali_property_rent": "rental_property",
"@bali_villa_sale": "sale_property"
```

### Общие чаты:
```json
"@bali_chat": "general",
"@bali_community": "general"
```

### Бизнес:
```json
"@bali_business": "general",
"@bali_entrepreneurs": "general"
```

---

## ⚠️ **ВАЖНО:**

1. ✅ Всегда используйте формат `@username` (с @)
2. ✅ Проверяйте запятые в JSON
3. ✅ Категория должна существовать в `messages.json`
4. ✅ Если категория не найдена → используется fallback (все сообщения)

---

## 🎉 **РЕЗУЛЬТАТ:**

После добавления групп в `group_niches.json`:
- ✅ Система автоматически выберет релевантные сообщения
- ✅ Группы получат посты из правильной категории
- ✅ Больше релевантности = больше конверсий!

---

**Система работает автоматически. Наслаждайтесь полетом! ✈️**

*Создано: 2026-01-13*
