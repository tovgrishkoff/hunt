# 🧠 Умный режим Secretary

## 📋 Принцип работы

Secretary теперь работает в **умном режиме**:
- ✅ **Первое сообщение** → отвечает через GPT автоматически
- ✅ **Активная переписка** → только пересылает на @grishkoff, **НЕ отвечает автоматически**

## 🔍 Как определяется активная переписка?

Система проверяет последние 10 сообщений в диалоге:
1. Ищет наше последнее сообщение (out=True)
2. Проверяет, есть ли сообщения от пользователя **ПОСЛЕ** нашего последнего
3. Если есть → это активная переписка, только пересылка
4. Если нет → это новое сообщение, отвечаем через GPT

## 📊 Логика работы

### Сценарий 1: Первое сообщение
```
Пользователь: "Привет, есть вопросы"
Secretary: [GPT ответ] "Привет! Конечно, задавай вопросы..."
```

### Сценарий 2: Активная переписка
```
Пользователь: "Привет"
Secretary: [GPT] "Привет! Чем могу помочь?"
Пользователь: "Хочу узнать про услуги"
Secretary: [GPT] "Конечно! У нас есть..."
Пользователь: "А сколько стоит?"
Secretary: [ТОЛЬКО ПЕРЕСЫЛКА на @grishkoff, НЕ отвечает]
```

## 🔧 Технические детали

### Метод `check_if_active_conversation()`
- Получает последние 10 сообщений
- Ищет наше последнее сообщение (out=True)
- Проверяет наличие ответов пользователя после него
- Возвращает `True` если идет переписка

### Метод `forward_message_to_grishkoff()`
- Формирует префикс с информацией об отправителе
- Отправляет префикс на @grishkoff
- Пересылает оригинальное сообщение
- Логирует действие

## 📝 Логирование

При обнаружении активной переписки:
```
💬 Active conversation detected with @username - forwarding only, no auto-reply
✅ Forwarded message from @username to @grishkoff
```

При обычном ответе:
```
📨 New DM from @username: ...
✅ Replied to @username: ...
```

## ⚙️ Настройки

Получатель пересылки задается в `FORWARD_TO_USERNAME = 'grishkoff'` (можно вынести в конфиг).

---

*Обновлено: 2026-01-10*
